# How Scapy Behaves Across the Packet Lifecycle — Build → Send → Sniff

This document answers, with **verbatim output captured by actually running Scapy from this working copy**, how Scapy behaves at runtime across the full packet lifecycle: **building** a multi‑layer `Ether/IP/TCP` packet, **sending** it, and **sniffing** the same traffic back. Scapy describes itself as *"a powerful Python-based interactive packet manipulation program and library"* (`README.md:L13-14`).

The user's question decomposes into four asks, each answered explicitly below and re‑checked in the **Coverage pass**:

- **R1** — Start Scapy and build a multi‑layer `Ether/IP/TCP` packet; observe the per‑layer display as each layer is added and how the final structure looks before sending. → **§1**
- **R2** — Send the packet using Scapy's usual mechanism; note routing information printed, what Scapy shows about the packet as it leaves, and any other transmission output. → **§2**
- **R3** — Sniff the same traffic; observe how Scapy presents/dissects the received bytes and reconstructs the layers when displaying. → **§3**
- **R4** — Summarize what Scapy revealed (connect the built packet → bytes on the wire → sniffed reconstruction). → **§4**

Every factual claim carries an exact `file:line` citation into this repository's source, or a verbatim observed‑output reference. Where a value is environment‑specific or could not be asserted universally, that is stated explicitly rather than implied.

---

## Environment & Reproducibility

All output below was produced on **this machine, from this working copy**, and re‑verified for this document. The values are what the tools **actually printed**, not what they "would" print.

| Item | Observed value | How confirmed |
|------|----------------|---------------|
| Python interpreter | **3.13.7** | `python -V` (the project venv `.venv/bin/python`) |
| Scapy version (`conf.version`) | **`2026.07.01`** | `from scapy.config import conf; conf.version` — dynamic, resolved via `pyproject.toml:L78` `version = { attr="scapy.VERSION" }` |
| Scapy import path | in‑repo working copy | `scapy.__file__` → `<repo>/scapy/__init__.py` |
| Repository HEAD | `0925ada4` | `git rev-parse --short HEAD` |
| Privilege | **root** | `whoami` |
| Socket backend | **native raw sockets** (`conf.use_pcap = False`) | libpcap/tcpdump absent; `conf.use_pcap` (`scapy/config.py:L832`, `use_pcap = Interceptor(`) |
| Verbosity default | **`conf.verb = 2`** | `scapy/config.py:L759` — `verb = 2  #: level of verbosity, from 0 (almost mute) to 3 (verbose)` |
| Loopback | `lo` UP, `127.0.0.1/8`, MAC `00:00:00:00:00:00` | `ip addr show lo` |
| `cryptography` | **`49.0.0`** present | `import cryptography; cryptography.__version__` |
| `IPython` | **`9.15.0`** present | `import IPython; IPython.__version__` |
| `six` | present | importable |
| Absent (graceful degradation) | `matplotlib`, `PyX` (`psdump()`/`pdfdump()` disabled), libpcap/tcpdump | INFO/ERROR lines shown below |

> **Note on values that differ from the task's example literals.** This working copy was exercised directly, and a few investigation‑note literals did not match what ran here. The values in **this** document are the ones observed on this machine:
> - **Python is `3.13.7`** here (an investigation note referenced `3.12.3`).
> - **`cryptography` is `49.0.0`** here, and importing Scapy **does emit** a `TripleDES` `CryptographyDeprecationWarning` (shown below). (An investigation note referenced `41.0.7` with no such warning.)
> - **`IPython 9.15.0` is present**, so the interactive banner uses the **IPython** shell path, not the "standard Python shell" fallback (both paths are shown in §1).
> - The **Scapy version `2026.07.01`**, the **IP checksum `0x7ccd`**, the **TCP checksum `0x917c`**, the **route tuple `('lo', '127.0.0.1', '0.0.0.0')`**, and the **sizes `40`/`54` bytes** all matched and are confirmed by the captures below.

### How the observations were produced

Three temporary scripts were placed in `/tmp` (outside the repository tree) and run against the in‑repo Scapy with `PYTHONPATH` set to the repository root:

```text
cd <repo-root>
PYTHONPATH=$PWD .venv/bin/python /tmp/obs_build.py    # R1: startup config + build/display
PYTHONPATH=$PWD .venv/bin/python /tmp/obs_send.py     # R2: routing + send verbosity
PYTHONPATH=$PWD .venv/bin/python /tmp/obs_sniff.py    # R3/R4: sniff round-trip + reconstruction
```

`PYTHONPATH=$PWD` is required because a script living in `/tmp` otherwise puts `/tmp` — not the repository — on `sys.path[0]`. These scripts were **deleted** afterwards; the repository is left byte‑for‑byte unchanged aside from this answer document.

---

## §1 — Starting Scapy & building the `Ether/IP/TCP` stack (answers R1)

### Starting Scapy

Scapy's interactive session is launched by `interact()` (`scapy/main.py:L503` — `def interact(mydict=None, argv=None, mybanner=None, loglevel=logging.INFO):`). It prints loading messages plus an ASCII banner. On this machine, running:

```text
PYTHONPATH=$PWD .venv/bin/python -c "from scapy.main import interact; interact()" </dev/null
```

emitted the following **loading messages on STDERR** (the absolute repository path prefix in the `CryptographyDeprecationWarning` has been normalized to the repo‑relative `scapy/...` — that prefix is environment‑specific):

```text
INFO: Can't import PyX. Won't be able to use psdump() or pdfdump().
INFO: No IPv6 support in kernel
scapy/layers/ipsec.py:573: CryptographyDeprecationWarning: TripleDES has been moved to cryptography.hazmat.decrepit.ciphers.algorithms.TripleDES and will be removed from cryptography.hazmat.primitives.ciphers.algorithms in 48.0.0.
  cipher=algorithms.TripleDES,
scapy/layers/ipsec.py:577: CryptographyDeprecationWarning: TripleDES has been moved to cryptography.hazmat.decrepit.ciphers.algorithms.TripleDES and will be removed from cryptography.hazmat.primitives.ciphers.algorithms in 48.0.0.
  cipher=algorithms.TripleDES,
```

and the **ASCII banner on STDOUT** (ANSI color codes — from `conf.color_theme`, a `DefaultTheme()` — were stripped for readability; they are present in the raw output):

```text
                                      
                     aSPY//YASa       
             apyyyyCY//////////YCa       |
            sY//////YSpcs  scpCY//Pp     | Welcome to Scapy
 ayp ayyyyyyySCP//Pp           syY//C    | Version 2026.07.01
 AYAsAYYYYYYYY///Ps              cY//S   |
         pCCCCY//p          cSSps y//Y   | https://github.com/secdev/scapy
         SPPPP///a          pP///AC//Y   |
              A//A            cyP////C   | Have fun!
              p///Ac            sC///a   |
              P////YCpc           A//A   | Craft packets like I craft my beer.
       scccccp///pSP///p          p//Y   |               -- Jean De Clerck
      sY/////////y  caa           S//P   |
       cayCyayP//Ya              pY/Ya
        sY/PsY////YCc          aC//Yp 
         sc  sccaCY//PCypaapyCP//YSs  
                  spCPY//////YPSps    
                       ccaacs         
                                       using IPython 9.15.0
Tip: You can use LaTeX or Unicode completion, `\alpha<tab>` will insert the α symbol.
>>> 
```

Observations, tied to what actually printed:

- The `| Version 2026.07.01` line is **dynamic**: it reflects `conf.version` (resolved from `scapy.VERSION` via `pyproject.toml:L78`).
- The banner ends with **`using IPython 9.15.0`** and a `Tip:` line, then the `>>> ` prompt — because **IPython is present** in this environment, Scapy uses the IPython shell (`scapy/main.py:L571-572` imports `IPython` / `start_ipython`).
- The quote after `Have fun!` is **random per run** — this run showed *"Craft packets like I craft my beer. -- Jean De Clerck"*; a second run showed a different quote (see below). Do not treat the quote as fixed.
- The two `INFO:` lines report the **graceful degradation** for absent optional dependencies (`PyX`, and no in‑kernel IPv6 here); Scapy continues normally.
- The `TripleDES CryptographyDeprecationWarning` (twice, from `scapy/layers/ipsec.py:573` and `:577`) is emitted by the `cryptography 49.0.0` import path; it is informational and does not stop startup.

**Fallback shell path (for completeness).** When IPython is *not* importable, `interact()` logs a warning and falls back to the standard Python shell (`scapy/main.py:L575-576`). Forcing that path on this machine (by making `import IPython` fail) produced the banner **on STDERR**, preceded by the warning — verbatim:

```text
INFO: Can't import PyX. Won't be able to use psdump() or pdfdump().
INFO: No IPv6 support in kernel
scapy/layers/ipsec.py:573: CryptographyDeprecationWarning: TripleDES has been moved to cryptography.hazmat.decrepit.ciphers.algorithms.TripleDES and will be removed from cryptography.hazmat.primitives.ciphers.algorithms in 48.0.0.
  cipher=algorithms.TripleDES,
scapy/layers/ipsec.py:577: CryptographyDeprecationWarning: TripleDES has been moved to cryptography.hazmat.decrepit.ciphers.algorithms.TripleDES and will be removed from cryptography.hazmat.primitives.ciphers.algorithms in 48.0.0.
  cipher=algorithms.TripleDES,
WARNING: IPython not available. Using standard Python shell instead.
AutoCompletion, History are disabled.
                                      
                     aSPY//YASa       
             apyyyyCY//////////YCa       |
            sY//////YSpcs  scpCY//Pp     | Welcome to Scapy
 ayp ayyyyyyySCP//Pp           syY//C    | Version 2026.07.01
 AYAsAYYYYYYYY///Ps              cY//S   |
         pCCCCY//p          cSSps y//Y   | https://github.com/secdev/scapy
         SPPPP///a          pP///AC//Y   |
              A//A            cyP////C   | Have fun!
              p///Ac            sC///a   |
              P////YCpc           A//A   | Craft packets before they craft
       scccccp///pSP///p          p//Y   | you.
      sY/////////y  caa           S//P   |                      -- Socrate
       cayCyayP//Ya              pY/Ya   |
        sY/PsY////YCc          aC//Yp 
         sc  sccaCY//PCypaapyCP//YSs  
                  spCPY//////YPSps    
                       ccaacs         
                                      

now exiting InteractiveConsole...
```

In that fallback run only the `>>> ` prompt went to STDOUT, and the session ended with `now exiting InteractiveConsole...`; the random quote that run was *"Craft packets before they craft you. -- Socrate"* (again confirming the quote varies).

A bare, non‑interactive `import scapy.all` (without `interact()`) does **not** print the INFO banner lines, but on this machine it is **not** completely silent: the two `TripleDES` `CryptographyDeprecationWarning` stanzas are written to STDERR (measured at **662 bytes**), because `cryptography 49.0.0` raises them on first use during import.

### Per‑layer display as each layer is added

The building block is `conf` and the layer classes, all imported by `from scapy.all import *`. The startup line printed by `/tmp/obs_build.py`:

```text
scapy.VERSION = 2026.07.01 | conf.verb = 2 | conf.use_pcap = False
Ether() -> <Ether  |>
Ether()/IP() -> <Ether  type=IPv4 |<IP  |>>
full stack -> <Ether  type=IPv4 |<IP  frag=0 proto=tcp dst=127.0.0.1 |<TCP  dport=http flags=S |>>>
```

The three `repr(...)` lines show exactly **how each layer is displayed as it is added**:

- `Ether()` → `<Ether  |>` — an empty `Ether()` shows **no fields at all**.
- `Ether()/IP()` → `<Ether  type=IPv4 |<IP  |>>` — stacking `IP` makes `type=IPv4` appear on the Ethernet layer.
- The full stack → `<Ether  type=IPv4 |<IP  frag=0 proto=tcp dst=127.0.0.1 |<TCP  dport=http flags=S |>>>`.

Why does an empty `Ether()` show nothing while stacking reveals `type=IPv4`? Because the `__repr__` renderer (`scapy/packet.py:L552` — `def __repr__(self):`) prints **only** fields that are explicitly set (present in `self.fields`) or overloaded by the payload (present in `self.overloaded_fields`), and it **skips unset fields and empty collections** (its loop does `if f.name in self.fields: ...elif f.name in self.overloaded_fields: ...else: continue`, and `continue`s when a list/dict/set value has `len == 0`). A freshly‑constructed `Ether()` has neither set nor overloaded fields, so nothing renders.

The `/` operator that stacks the layers is `__div__` (`scapy/packet.py:L596` — `def __div__(self, other):`, aliased to `__truediv__`): it copies both operands and calls `add_payload`. Stacking **overloads the lower layer's demultiplexing fields** — so `Ether.type` becomes `IPv4` and `IP.proto` becomes `tcp`, i.e. the upper layers make the lower layers self‑describe. The layer defaults come from `Ether` (`scapy/layers/l2.py:L244` — `class Ether(Packet):`), `IP` (`scapy/layers/inet.py:L521` — `class IP(Packet, IPTools):`) and `TCP` (`scapy/layers/inet.py:L753` — `class TCP(Packet):`).

The packet under study — the user's *"simple multi‑layer packet such as an Ethernet/IP/TCP stack"* — is:

```text
>>> pkt = Ether()/IP(dst="127.0.0.1")/TCP(dport=80, flags="S")
```

### `summary()` — one‑line description

```text
>>> print(pkt.summary())
Ether / IP / TCP 127.0.0.1:ftp_data > 127.0.0.1:http S
```

`summary()` (`scapy/packet.py:L1642` — `def summary(self, intern=0):`, using `mysummary` at `scapy/packet.py:L1609`) renders symbolic service names: the TCP source port defaults to `20` whose service name is `ftp_data`, the destination `80` is `http`, and `flags="S"` shows as `S`.

### `show()` — hierarchical, symbolic, computed fields still `None`

```text
>>> pkt.show()
###[ Ethernet ]### 
  dst       = ff:ff:ff:ff:ff:ff
  src       = 00:00:00:00:00:00
  type      = IPv4
###[ IP ]### 
     version   = 4
     ihl       = None
     tos       = 0x0
     len       = None
     id        = 1
     flags     = 
     frag      = 0
     ttl       = 64
     proto     = tcp
     chksum    = None
     src       = 127.0.0.1
     dst       = 127.0.0.1
     \options   \
###[ TCP ]### 
        sport     = ftp_data
        dport     = http
        seq       = 0
        ack       = 0
        dataofs   = None
        reserved  = 0
        flags     = S
        window    = 8192
        chksum    = None
        urgptr    = 0
        options   = ''
```

`show()` (`scapy/packet.py:L1459` — `def show(self, dump=False, indent=3, lvl="", label_lvl=""):`) is a **symbolic** view. Note two things:

- **Symbolic values**: `proto = tcp`, `dport = http`, `sport = ftp_data` (names, not numbers).
- **Deferred/computed fields are still `None` before sending**: on `IP`, `ihl = None`, `len = None`, `chksum = None`; on `TCP`, `dataofs = None`, `chksum = None`. Scapy has not computed lengths or checksums yet — those are filled in only when the packet is assembled to bytes.

### `ls(pkt)` — numeric, typed view

```text
>>> ls(pkt)
dst        : DestMACField                        = 'ff:ff:ff:ff:ff:ff' ('None')
src        : SourceMACField                      = '00:00:00:00:00:00' ('None')
type       : XShortEnumField                     = 2048            ('36864')
--
version    : BitField  (4 bits)                  = 4               ('4')
ihl        : BitField  (4 bits)                  = None            ('None')
tos        : XByteField                          = 0               ('0')
len        : ShortField                          = None            ('None')
id         : ShortField                          = 1               ('1')
flags      : FlagsField                          = <Flag 0 ()>     ('<Flag 0 ()>')
frag       : BitField  (13 bits)                 = 0               ('0')
ttl        : ByteField                           = 64              ('64')
proto      : ByteEnumField                       = 6               ('0')
chksum     : XShortField                         = None            ('None')
src        : SourceIPField                       = '127.0.0.1'     ('None')
dst        : DestIPField                         = '127.0.0.1'     ('None')
options    : PacketListField                     = []              ('[]')
--
sport      : ShortEnumField                      = 20              ('20')
dport      : ShortEnumField                      = 80              ('80')
seq        : IntField                            = 0               ('0')
ack        : IntField                            = 0               ('0')
dataofs    : BitField  (4 bits)                  = None            ('None')
reserved   : BitField  (3 bits)                  = 0               ('0')
flags      : FlagsField                          = <Flag 2 (S)>    ('<Flag 2 (S)>')
window     : ShortField                          = 8192            ('8192')
chksum     : XShortField                         = None            ('None')
urgptr     : ShortField                          = 0               ('0')
options    : TCPOptionsField                     = []              ("b''")
```

`ls()` lists each field's **class** and its **numeric/typed value**, with the field default in parentheses. Contrast this with `show()`: here `type` is `2048` (not `IPv4`), `proto` is `6` (not `tcp`), `dport` is `80` (not `http`), and `sport` is `20`. The still‑unresolved computed fields again read `None` (`ihl`, `len`, `chksum`, `dataofs`).

### `show2()` — assembles the packet, so computed fields resolve

```text
>>> pkt.show2()
###[ Ethernet ]### 
  dst       = ff:ff:ff:ff:ff:ff
  src       = 00:00:00:00:00:00
  type      = IPv4
###[ IP ]### 
     version   = 4
     ihl       = 5
     tos       = 0x0
     len       = 40
     id        = 1
     flags     = 
     frag      = 0
     ttl       = 64
     proto     = tcp
     chksum    = 0x7ccd
     src       = 127.0.0.1
     dst       = 127.0.0.1
     \options   \
###[ TCP ]### 
        sport     = ftp_data
        dport     = http
        seq       = 0
        ack       = 0
        dataofs   = 5
        reserved  = 0
        flags     = S
        window    = 8192
        chksum    = 0x917c
        urgptr    = 0
        options   = ''
```

`show2()` (`scapy/packet.py:L1473` — `def show2(self, dump=False, indent=3, lvl="", label_lvl=""):`) differs from `show()` because it **assembles the packet first** (via `self.__class__(raw(self)).show(...)`), which forces the `build`/`do_build` machinery (`scapy/packet.py:L746` — `def build(self):`; `scapy/packet.py:L724` — `def do_build(self):`) to compute lengths and checksums, then re‑dissects. The previously‑`None` fields now **resolve**:

- `IP`: `ihl = 5`, `len = 40`, `chksum = 0x7ccd`.
- `TCP`: `dataofs = 5`, `chksum = 0x917c`.

### `raw(pkt)` — the exact bytes before sending

```text
>>> rb = raw(pkt); print("len(raw(pkt)) =", len(rb)); print("raw hex =", rb.hex())
len(raw(pkt)) = 54
raw hex = ffffffffffff0000000000000800450000280001000040067ccd7f0000017f00000100140050000000000000000050022000917c0000
```

The assembled frame is **54 bytes** = 14 (Ethernet) + 20 (IP) + 20 (TCP); the IP `len` field carries `40` (IP + TCP). The computed checksums are visible in the byte stream: `...7ccd...` (IP) and `...917c` (TCP, the last two payload bytes before the trailing `0000`).

**Key insight — building does not mutate the original packet.** `show2()`/`build()` compute on an *assembled copy*. After calling them, the original `pkt` **still** reports `ihl=None, len=None, chksum=None` (IP) and `dataofs=None, chksum=None` (TCP) via `show()`. That is precisely why `show()` and `show2()` differ even after `show2()` has run: the deferred fields are resolved on the copy, not written back to `pkt`.


---

## §2 — Sending & observing transmission (answers R2)

### Routing information Scapy prints/uses

Scapy consults **its own IPv4 routing table**, `conf.route` (class `Route` at `scapy/route.py:L31` — `class Route:`; the config slot is `scapy/config.py:L806` — `route = None`). Printing the table and resolving the loopback destination produced:

```text
>>> print(conf.route)
>>> print(repr(conf.route.route("127.0.0.1")))
Network     Netmask          Gateway     Iface    Output IP     Metric
0.0.0.0     0.0.0.0          10.236.7.1  eth0     10.236.7.192  0     
10.236.7.0  255.255.255.0    10.236.7.1  eth0     10.236.7.192  0     
10.236.7.1  255.255.255.255  0.0.0.0     eth0     10.236.7.192  0     
127.0.0.0   255.0.0.0        0.0.0.0     lo       127.0.0.1     1     
172.17.0.0  255.255.0.0      0.0.0.0     docker0  172.17.0.1    0     
('lo', '127.0.0.1', '0.0.0.0')
```

Two levels of fact here:

- **Portable / stable facts** (the same anywhere loopback is configured):
  - `conf.route.route("127.0.0.1")` returns the tuple **`('lo', '127.0.0.1', '0.0.0.0')`**. This is `(iface, output_ip, gateway_ip)`, exactly as documented by `Route.route` (`scapy/route.py:L146` — `def route(self, dst=None, verbose=conf.verb):`, whose docstring states `returns: (iface, output_ip, gateway_ip)`). The loopback destination resolves to interface **`lo`**, output IP **`127.0.0.1`**, and gateway **`0.0.0.0`** (i.e. directly connected, no gateway).
  - The loopback row of the table: `127.0.0.0   255.0.0.0   0.0.0.0   lo   127.0.0.1   1`.
- **Environment‑specific facts** (these *will differ per host* — do not treat as universal): the table's columns are `Network / Netmask / Gateway / Iface / Output IP / Metric`, and on this machine it also listed `eth0` rows (output IP `10.236.7.192`, gateway `10.236.7.1`) and a `docker0` row (`172.17.0.1`). Another host will show different addresses.

### Sending the packet and the transmission output

`send()` (`scapy/sendrecv.py:L422` — `def send(x, ...`) transmits at **layer 3** (the caller supplies no Ethernet header; Scapy uses the L3 socket). Its layer‑2 counterpart is `sendp()` (`scapy/sendrecv.py:L452` — `def sendp(x, ...`). Both delegate to the internal `__gen_send` (`scapy/sendrecv.py:L332` — `def __gen_send(s, ...`).

Transmission output is **gated by `conf.verb`** (default `2`, `scapy/config.py:L759`): inside `__gen_send`, `if verbose is None: verbose = conf.verb`. When verbose, it writes **one `.` per packet** via `os.write(1, b".")` (`scapy/sendrecv.py:L380`) and prints the summary line via `print("\nSent %i packets." % n)` (`scapy/sendrecv.py:L392`).

Running a single default `send()` in an interactive (TTY) session produced exactly:

```text
>>> send(IP(dst="127.0.0.1")/TCP(dport=80, flags="S"))
.
Sent 1 packets.
```

i.e. the progress marker `.` followed by `Sent 1 packets.` — Scapy sent **1** packet and said so.

**Output‑ordering nuance (stated honestly).** `os.write(1, b".")` writes **unbuffered** straight to file descriptor 1, whereas `print()` uses Python's **buffered** stdout. In an interactive terminal they appear in order (`.` then `Sent 1 packets.`, as above). But when STDOUT is **redirected to a file/pipe**, the buffered `print` output is flushed later, so the unbuffered `.` can appear **clustered/out of order** relative to the buffered text. On this machine, redirecting the send script to a file put the `.` at the very top of the capture — the first line read:

```text
.conf.verb = 2
```

That single leading `.` is the send's progress marker landing ahead of the buffered `print("conf.verb =", conf.verb)` output. The interleaving therefore depends on whether stdout is a TTY; it is not a fixed sequence.

**Verbosity gate, demonstrated.** Calling `send(..., verbose=0)` printed **nothing at all** — no `.`, no `Sent` line. In the redirected run there was exactly **one** `.` and **one** `Sent 1 packets.` line total (from the first, default‑verbose call); the second `verbose=0` call was silent. This confirms `conf.verb` (and the per‑call `verbose=` override) fully controls the transmission chatter.

**Socket backend.** Because libpcap/tcpdump is absent, sends use **native raw sockets** (`conf.use_pcap = False`, `scapy/config.py:L832`), implemented by Scapy's socket layer (`scapy/supersocket.py`) and the Linux backend (`scapy/arch/linux.py`). No external transmission occurs: the packet is confined to the local host via `lo`.


---

## §3 — Sniffing & observing reconstruction (answers R3)

### Why a Python `lfilter`, not a BPF `filter=` string

A BPF `filter=` string requires libpcap, which is absent here. Attempting it does **not** raise — it logs an `ERROR` to STDERR and returns an **empty** result:

```text
>>> sniff(iface='lo', filter='tcp and port 80', count=1, timeout=3)
ERROR: Cannot set filter: libpcap is not available. Cannot compile filter !
<Sniffed: TCP:0 UDP:0 ICMP:0 Other:0>
```

The returned `<Sniffed: TCP:0 UDP:0 ICMP:0 Other:0>` object confirms zero packets were captured. The workaround is a native capture with a **Python `lfilter` callable** instead of a BPF string.

### Capturing the round trip with `AsyncSniffer`

`sniff()` (`scapy/sendrecv.py:L1308` — `def sniff(*args, **kwargs):`) and its threaded form `AsyncSniffer` were used to capture on `lo` while concurrently sending the same packet. The pattern:

```text
>>> built = IP(dst="127.0.0.1")/TCP(dport=80, flags="S"); raw_built = raw(built)
>>> t = AsyncSniffer(iface="lo", lfilter=lambda p: TCP in p and p[TCP].dport == 80, count=1, timeout=6)
>>> t.start(); time.sleep(1); send(built, verbose=0); time.sleep(1); t.join()
>>> cap = t.results[0]
```

Captured packets are read from **`t.results`** after `.join()`. Note the sniffer **auto‑stops** once `count` is reached, so an explicit `.stop()` afterwards raises — verbatim:

```text
>>> t.stop()
STOP-EXC: Scapy_Exception -> Not running ! (check .running attr)
```

This is the guard `raise Scapy_Exception("Not running ! (check .running attr)")` at `scapy/sendrecv.py:L1299`. The lesson: read `sniffer.results` after `.join()`; do not call `.stop()` on an already‑stopped sniffer.

### How Scapy presents and reconstructs the received bytes

Scapy **dissects** the raw bytes back into layers (`dissect`, `scapy/packet.py:L1049` — `def dissect(self, s):`). The captured frame reconstructs cleanly:

```text
>>> [l.__name__ for l in cap.layers()]
['Ether', 'IP', 'TCP']
>>> cap.summary()
Ether / IP / TCP 127.0.0.1:ftp_data > 127.0.0.1:http S
```

`cap.show()` on the **sniffed** frame shows **every field resolved — no `None`** — because these bytes came off the wire fully formed (lengths and checksums already present):

```text
>>> cap.show()
###[ Ethernet ]### 
  dst       = ff:ff:ff:ff:ff:ff
  src       = 00:00:00:00:00:00
  type      = IPv4
###[ IP ]### 
     version   = 4
     ihl       = 5
     tos       = 0x0
     len       = 40
     id        = 1
     flags     = 
     frag      = 0
     ttl       = 64
     proto     = tcp
     chksum    = 0x7ccd
     src       = 127.0.0.1
     dst       = 127.0.0.1
     \options   \
###[ TCP ]### 
        sport     = ftp_data
        dport     = http
        seq       = 0
        ack       = 0
        dataofs   = 5
        reserved  = 0
        flags     = S
        window    = 8192
        chksum    = 0x917c
        urgptr    = 0
        options   = ''
```

Compare this with the pre‑send `show()` in §1: there, `ihl`, `len`, `chksum`, `dataofs` were `None`; here they are `ihl = 5`, `len = 40`, IP `chksum = 0x7ccd`, `dataofs = 5`, TCP `chksum = 0x917c`. Dissection recovered the exact values Scapy computed at build time.

`hexdump(cap)` shows the 54 bytes of the frame. The 14‑byte Ethernet header occupies offsets `0x0000–0x000D`, and the IP header begins at offset `0x000E` with `45 00 00 28` (`45` = IP version 4 / IHL 5; `00 28` = total length `40`):

```text
>>> hexdump(cap)
0000  FF FF FF FF FF FF 00 00 00 00 00 00 08 00 45 00  ..............E.
0010  00 28 00 01 00 00 40 06 7C CD 7F 00 00 01 7F 00  .(....@.|.......
0020  00 01 00 14 00 50 00 00 00 00 00 00 00 00 50 02  .....P........P.
0030  20 00 91 7C 00 00                                 ..|..
```

**Honest note on the Ethernet framing.** The packet was sent at layer 3 with `send()`, yet the captured frame carries an Ethernet header whose bytes equal the **default `Ether()` values** (`dst=ff:ff:ff:ff:ff:ff`, `src=00:00:00:00:00:00`, `type=0x0800`). This is the link‑layer framing observed on `lo` in this environment; the mechanism by which those specific default bytes appear on the loopback frame is not something these captures alone prove, so it is reported as observed rather than over‑claimed. What *is* proven (next section) is that the IP/TCP payload is byte‑identical to the built packet.


---

## §4 — Summary: build → wire → sniff continuity (answers R4)

Putting the three phases together, the same information is visible at **three stages** — a symbolic build‑time view, the raw bytes on the wire, and the dissected reconstruction — and they line up exactly.

**Sizes.** The built layer‑3 payload `raw(IP(dst="127.0.0.1")/TCP(dport=80, flags="S"))` is **40 bytes**; the full built frame `raw(Ether()/IP(...)/TCP(...))` is **54 bytes**. The sniffed frame is likewise 54 bytes.

**Byte‑for‑byte continuity (measured, not asserted).** The sniff script compared the sniffed packet against the built one:

```text
raw(cap[IP]) == raw(built) ? True
raw(cap) == raw(Ether()/built) ? True
len(raw(built)) = 40 | len(raw(cap)) = 54
```

- `raw(cap[IP]) == raw(built)` → **`True`**: the IP/TCP portion of the sniffed frame equals the built layer‑3 payload byte‑for‑byte.
- `raw(cap) == raw(Ether()/built)` → **`True`**: the *entire* 54‑byte sniffed frame equals the built full frame.
- `len(raw(built)) = 40 | len(raw(cap)) = 54` confirms the 40‑byte payload inside the 54‑byte frame.

**Matching checksums prove the round trip.** The IP checksum **`0x7ccd`** and the TCP checksum **`0x917c`** are identical in the built `show2()` (§1) and the sniffed `show()` (§3), and the IP `len` field is `40` in both. The full built frame's hex from §1 —

```text
ffffffffffff0000000000000800450000280001000040067ccd7f0000017f00000100140050000000000000000050022000917c0000
```

— is exactly the 54 bytes that came back from the sniffer (hence `raw(cap) == raw(Ether()/built)` is `True`).

**Tying it back to display.** `show()` deferred the computed fields (`ihl`, `len`, `chksum`, `dataofs`) as `None` before sending; `build()`/`raw()` computed them into the wire bytes; and `show()` on the sniffed frame displays them **resolved**. Same packet, viewed at three stages: **symbolic build view → bytes on the wire → dissected reconstruction**, with the checksums (`0x7ccd`, `0x917c`) and the 40‑byte payload proving they are one and the same.

---

## Coverage pass

Each part of the user's question, mapped to where it is answered with verbatim evidence:

| User ask / sub‑phrase | Where answered | Verbatim evidence |
|-----------------------|----------------|-------------------|
| R1 — "start Scapy" | §1 *Starting Scapy* | IPython banner (STDOUT) + INFO/warnings (STDERR); `interact()` `scapy/main.py:L503` |
| R1 — "what Scapy displays as each layer is added" | §1 *Per‑layer display* | `<Ether  \|>`, `<Ether  type=IPv4 \|<IP  \|>>`, full stack repr; `__repr__` `scapy/packet.py:L552`, `__div__` `scapy/packet.py:L596` |
| R1 — "how the final structure looks before sending" | §1 *summary/show/ls/show2/raw* | `summary()`, `show()` (with `None`), `ls()` (numeric), `show2()` (resolved), `raw` hex = 54 bytes |
| R2 — "routing information it prints" | §2 *Routing* | `conf.route` table + `route("127.0.0.1")` → `('lo', '127.0.0.1', '0.0.0.0')`; `scapy/route.py:L146` |
| R2 — "what it shows about the packet as it leaves" | §2 *Sending* | `.` marker (`scapy/sendrecv.py:L380`) + `Sent 1 packets.` (`scapy/sendrecv.py:L392`) |
| R2 — "any other output while sending" | §2 *Verbosity gate / ordering* | `verbose=0` silence; buffered/unbuffered ordering nuance |
| R3 — "how it interprets the data" | §3 *Reconstruction* | BPF ERROR + empty `<Sniffed: ...>`; `dissect` `scapy/packet.py:L1049` |
| R3 — "how it reconstructs the layers when showing the packet" | §3 *Reconstruction* | `layers()` → `['Ether', 'IP', 'TCP']`, resolved `show()`, `hexdump()` |
| R4 — "summarize … built → wire → sniffed" | §4 | `raw(cap[IP]) == raw(built)` → `True`; matching `0x7ccd`/`0x917c`; sizes 40/54 |

**Exactness pass.** Every measured value the question touches is quoted verbatim above with a source: sizes **`54`**/**`40`**; count **`Sent 1 packets.`**; checksums **`0x7ccd`** (IP) / **`0x917c`** (TCP); route tuple **`('lo', '127.0.0.1', '0.0.0.0')`**; the BPF error string; and the stop guard string **`Not running ! (check .running attr)`**.

**Honesty pass (environment‑specific / not universally assertable).** The following are explicitly *not* claimed as universal: the full routing table rows (host‑specific addresses), the random banner quote (varies per run), the exact `cryptography`/`IPython` versions and whether the `TripleDES` warning appears (depend on the installed `cryptography` build), and the precise mechanism producing the default‑`Ether()` link‑layer framing seen on `lo`.

---

## Version note (authoritative vs. stale)

- **Authoritative:** `pyproject.toml:L17` — `requires-python = ">=3.7, <4"`. This governs runtime Python support; the observations here on Python **3.13.7** fall within it.
- **Stale:** `README.md:L29` — *"Scapy supports Python 2.7 and Python 3 (3.4 to 3.9)."* This is out of date relative to `pyproject.toml` and should not be relied upon for runtime decisions.

---

## Citation table

| Symbol / fact | Location |
|---------------|----------|
| `__repr__` (renders only set/overloaded fields) | `scapy/packet.py:L552` |
| `__div__` (the `/` stacking operator) | `scapy/packet.py:L596` |
| `do_build` | `scapy/packet.py:L724` |
| `build` | `scapy/packet.py:L746` |
| `dissect` | `scapy/packet.py:L1049` |
| `show` | `scapy/packet.py:L1459` |
| `show2` | `scapy/packet.py:L1473` |
| `mysummary` | `scapy/packet.py:L1609` |
| `summary` | `scapy/packet.py:L1642` |
| `__gen_send` (verbosity, `.` marker, `Sent` line) | `scapy/sendrecv.py:L332` |
| `os.write(1, b".")` | `scapy/sendrecv.py:L380` |
| `print("\nSent %i packets." % n)` | `scapy/sendrecv.py:L392` |
| `send` (layer 3) | `scapy/sendrecv.py:L422` |
| `sendp` (layer 2) | `scapy/sendrecv.py:L452` |
| `AsyncSniffer` stop guard | `scapy/sendrecv.py:L1299` |
| `sniff` | `scapy/sendrecv.py:L1308` |
| `Route` class | `scapy/route.py:L31` |
| `Route.route` → `(iface, output_ip, gateway_ip)` | `scapy/route.py:L146` |
| `conf.verb = 2` | `scapy/config.py:L759` |
| `conf.route = None` (slot) | `scapy/config.py:L806` |
| `conf.use_pcap` (Interceptor) | `scapy/config.py:L832` |
| `interact()` (startup banner) | `scapy/main.py:L503` |
| IPython import / fallback warning | `scapy/main.py:L571-576` |
| `Ether` layer defaults | `scapy/layers/l2.py:L244` |
| `IP` layer defaults | `scapy/layers/inet.py:L521` |
| `TCP` layer defaults | `scapy/layers/inet.py:L753` |
| native raw‑socket backends | `scapy/supersocket.py`, `scapy/arch/linux.py` |
| `requires-python` (authoritative) | `pyproject.toml:L17` |
| dynamic `version = { attr="scapy.VERSION" }` | `pyproject.toml:L78` |
| project description | `README.md:L13-14` |
| stale Python‑support note | `README.md:L29` |

---

*Produced by running Scapy `2026.07.01` from this working copy (HEAD `0925ada4`) on Python `3.13.7`, as `root`, over the loopback interface `lo` with native raw sockets (`conf.use_pcap = False`) and `conf.verb = 2`. All console blocks above are verbatim captures; temporary observation scripts used to produce them were removed afterward.*

