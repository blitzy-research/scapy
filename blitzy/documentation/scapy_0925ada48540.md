# Scapy Runtime Investigation — Q&A

This document answers seven questions about the [Scapy](https://github.com/secdev/scapy) packet‑manipulation library by **running the code first and writing from the observed output**, then explaining each behavior against the source with exact `file:line` citations.

- **Checkout path:** `/tmp/blitzy/scapy/blitzy-fe4437a6-27e6-4b3a-ac0f-97db5358536b_a9e597`
- **Commit:** `0925ada485406684174d6f068dbd85c4154657b3` (verified `HEAD`)
- **Branch:** `scapy_0925ada48540` (the source branch this document is named after)
- **Interpreter:** Python 3.13.7
- **Privilege:** run as `root` (uid 0) — required for the raw‑socket sends in Q5

> **Methodology (run‑first).** Every command shown below was executed in the checkout above, and the fenced *output* blocks are the actual, unedited output of that run (ANSI color codes are stripped only where noted, for readability). Genuinely variable items (the random startup quote, kernel‑assigned reply IDs/checksums, run durations) are reported as *my* observed values and flagged as varying; deterministic items (the version string, the assembled packet bytes, the IP/ICMP checksums) reproduce exactly.

---

## Runtime / Environment Context

The environment differs in a few ways from a "released" Scapy install, and those differences directly explain several observations below (the startup banner lines and many test outcomes). They are reported here, not "corrected."

| Item | Observed value | Relevance |
|------|----------------|-----------|
| Python | **3.13.7** | Newer than the manifest's documented range `requires-python = ">=3.7, <4"` (`pyproject.toml:L17`); classifiers list 3.7–3.10. Scapy nonetheless runs correctly. |
| scapy | **2026.07.06** | A *computed* mtime‑fallback version (see Q2), **not** a released tag. |
| cryptography | **43.0.0** | Installed. Backs `scapy/layers/ipsec.py` and the TLS layer. Source of the `TripleDES` deprecation warnings in the Q1 banner **and** the removed‑module import errors behind the TLS test failures in Q7. |
| zstandard | 0.25.0 | Installed (optional compression). |
| IPython | **not installed** | ⇒ the `WARNING: IPython not available…` banner line; the standard Python REPL is used. |
| PyX | **not installed** | ⇒ the `INFO: Can't import PyX…` banner line. |
| python‑can | **not installed** | ⇒ the ISO‑TP / SocketCAN test failures in Q7. |
| matplotlib / mock | **not installed** | ⇒ various `regression.uts` test failures in Q7. |
| git tags | **0** | ⇒ `git describe` yields a bare SHA, which is why Q2's version falls back to the file mtime. |

---

## Q1 — Start the Scapy shell and show what the startup looks like

**Direct answer:** launching the canonical console prints a few `INFO`/`WARNING` layer‑loading lines (and, in this environment, two `cryptography` deprecation warnings), followed by the ASCII‑art welcome banner ending at the `>>>` prompt.

**Command (non‑interactive, so it cannot hang in the REPL):**

```bash
printf 'exit()\n' | python3 -m scapy
```

This is the *canonical* entry point. The Unix launcher `run_scapy` is simply `PYTHONPATH=$DIR exec "$PYTHON" -m scapy $@` (`run_scapy:L13`), and the packaging console‑script is `scapy = "scapy.main:interact"` (`pyproject.toml:L47`). Both land in `interact()` (`scapy/main.py:L503`).

**Output (ANSI color escapes stripped for readability — see nuance 4):**

```text
INFO: Can't import PyX. Won't be able to use psdump() or pdfdump().
INFO: No IPv6 support in kernel
/tmp/blitzy/scapy/blitzy-fe4437a6-27e6-4b3a-ac0f-97db5358536b_a9e597/scapy/layers/ipsec.py:573: CryptographyDeprecationWarning: TripleDES has been moved to cryptography.hazmat.decrepit.ciphers.algorithms.TripleDES and will be removed from this module in 48.0.0.
  cipher=algorithms.TripleDES,
/tmp/blitzy/scapy/blitzy-fe4437a6-27e6-4b3a-ac0f-97db5358536b_a9e597/scapy/layers/ipsec.py:577: CryptographyDeprecationWarning: TripleDES has been moved to cryptography.hazmat.decrepit.ciphers.algorithms.TripleDES and will be removed from this module in 48.0.0.
  cipher=algorithms.TripleDES,
WARNING: IPython not available. Using standard Python shell instead.
AutoCompletion, History are disabled.
                                      
                     aSPY//YASa       
             apyyyyCY//////////YCa       |
            sY//////YSpcs  scpCY//Pp     | Welcome to Scapy
 ayp ayyyyyyySCP//Pp           syY//C    | Version 2026.07.06
 AYAsAYYYYYYYY///Ps              cY//S   |
         pCCCCY//p          cSSps y//Y   | https://github.com/secdev/scapy
         SPPPP///a          pP///AC//Y   |
              A//A            cyP////C   | Have fun!
              p///Ac            sC///a   |
              P////YCpc           A//A   | Craft packets before they craft
       scccccp///pSP///p          p//Y   | you.
      sY/////////y  caa           S//P   |                      -- Socrate
       cayCyayP//Ya              pY/Ya   |
        sY/PsY////YCc          aC//Yp 
         sc  sccaCY//PCypaapyCP//YSs  
                  spCPY//////YPSps    
                       ccaacs         
                                      
>>> now exiting InteractiveConsole...
```

(The final `now exiting InteractiveConsole...` is the standard‑REPL response to the piped `exit()`.)

**Causal explanation (what emits each part):**

- **The ASCII banner** is built from the `the_banner` list (`scapy/main.py:L628-L639`), including `"   | Welcome to Scapy"` (`scapy/main.py:L632`) and the version line `"   | Version %s" % conf.version` (`scapy/main.py:L633`). The logo and text columns are colorized and interleaved before being handed to the REPL.
- **`INFO: Can't import PyX…`** comes from `log_loading.info(...)` in `scapy/libs/test_pyx.py:L45` because PyX is not installed.
- **`INFO: No IPv6 support in kernel`** comes from `log_loading.info("No IPv6 support in kernel")` in `scapy/route6.py:L66`.
- **`WARNING: IPython not available. Using standard Python shell instead.\nAutoCompletion, History are disabled.`** — `interact()` attempts `import IPython`; because IPython is absent the `ImportError` path fires `log_loading.warning(...)` (`scapy/main.py:L574-L577`) and sets `IPYTHON = False` (`scapy/main.py:L583`), so the standard REPL is used via `code.interact(banner=banner_text, local=SESSION)` (`scapy/main.py:L708`).
- **The two `CryptographyDeprecationWarning: TripleDES …` lines** are emitted while the IPsec layer defines its DES/3DES cipher algorithms with `cipher=algorithms.TripleDES` at `scapy/layers/ipsec.py:L573` and `scapy/layers/ipsec.py:L577`.

**Nuances (each verified on my run):**

1. **The final quote/author line is RANDOM.** It is selected by `quote, author = choice(QUOTES)` (`scapy/main.py:L646`) from the `QUOTES` list (`scapy/main.py:L49`) and rendered by `_prepare_quote()` (`scapy/main.py:L476`), so it differs each launch. The banner above shows `Craft packets before they craft you. -- Socrate`; six consecutive launches produced five distinct quotes, e.g. `Craft me if you can. -- IPv6 layer`, `What is dead may never die! -- Python 2`, `Craft packets like I craft my beer. -- Jean De Clerck`, `Wanna support scapy? Star us on GitHub! -- Satoshi Nakamoto`, and `To craft a packet, you have to be a packet… -- Jean-Claude Van Damme`. **Do not treat the quote as fixed.**
2. **The `INFO`/`WARNING` lines are a consequence of optional packages being absent** (PyX, IPython) — they are informational, not errors.
3. **This environment *does* emit the `TripleDES CryptographyDeprecationWarning` (twice).** With `cryptography 43.0.0`, `algorithms.TripleDES` is deprecated, so the warning fires at `ipsec.py:L573` and `:L577`. (An older `cryptography` such as 41.0.7 would not emit it — the warning presence is a function of the installed `cryptography` version, reported here as actually observed.)
4. **ANSI color escape codes are present even when piped.** `interact()` colorizes the banner via `conf.color_theme`, so the raw stream contains escapes like `\x1b[32m\x1b[1m` (green logo), `\x1b[34m\x1b[1m` (blue text) and `\x1b[33m\x1b[1m` (yellow warning); they were stripped above only for readability.

---

## Q2 — What version is it running?

**Direct answer:** `2026.07.06`.

**Commands:**

```bash
python3 -c "import scapy; print('scapy.VERSION    =', scapy.VERSION)"
python3 -c "import scapy; print('scapy.__version__=', scapy.__version__)"
python3 -c "from scapy.all import conf; print('conf.version     =', conf.version)"
```

**Output:**

```text
scapy.VERSION    = 2026.07.06
scapy.__version__= 2026.07.06
conf.version     = 2026.07.06
```

**Mechanism (what computes the value):** the version is computed at import time by `_version()` (`scapy/__init__.py:L122-L169`), assigned via `VERSION = __version__ = _version()` (`scapy/__init__.py:L169`), and surfaced on the config object as `version = ReadOnlyAttribute("version", VERSION)` (`scapy/config.py:L726`, which does `from scapy import VERSION` at `scapy/config.py:L22`). The packaging manifest reads it dynamically too: `version = { attr="scapy.VERSION" }` (`pyproject.toml:L78`).

`_version()` tries four sources in order and takes the first that succeeds. I traced each one at runtime:

```bash
python3 /tmp/trace_version.py   # calls the four helpers directly and prints which fires
```

**Output:**

```text
Method 0 (SCAPY_VERSION env): None -> UNSET, skipped
Method 1 (bundled scapy/VERSION file): exists = False -> skipped
Method 2 (_version_from_git_archive): raised ValueError -> not a git archive
Method 3 (_version_from_git_describe): raised ValueError -> tag has invalid format
FINAL _version() -> '2026.07.06'
```

- **Method 0 — `SCAPY_VERSION` environment variable:** unset ⇒ skipped (checked at `scapy/__init__.py:L131`).
- **Method 1 — bundled `scapy/VERSION` file:** does not exist ⇒ skipped (`scapy/__init__.py:L135-L143`).
- **Method 2 — `_version_from_git_archive()`** (`scapy/__init__.py:L147`): raises `ValueError('not a git archive')` (the `$Format:…$` placeholder is still literal in a live checkout), raised at `scapy/__init__.py:L56`.
- **Method 3 — `_version_from_git_describe()`** (`scapy/__init__.py:L153`): this checkout has **0 git tags**, so `git describe` returns a bare SHA that fails `_parse_tag()`'s regex, raising `ValueError('tag has invalid format')` at `scapy/__init__.py:L35`.
- **⇒ Fallback — file mtime date** (`scapy/__init__.py:L159-L161`): `d = datetime.datetime.utcfromtimestamp(os.path.getmtime(__file__))` then `return d.strftime('%Y.%m.%d')`.

Corroborating the bare‑SHA point:

```bash
git tag | wc -l          # -> 0
git describe --always    # -> 0925ada4   (a bare short SHA, not a tag)
```

**Evidence that the fallback actually computes the date from the file mtime:**

```bash
python3 -c "import os,datetime; f='scapy/__init__.py'; m=os.path.getmtime(f); \
print(repr(m)); print(datetime.datetime.utcfromtimestamp(m)); \
print(datetime.datetime.utcfromtimestamp(m).strftime('%Y.%m.%d'))"
```

```text
1783375852.4577055
2026-07-06 22:10:52.457705
2026.07.06
```

And a runtime proof that **line 160 specifically executes** at import: running with all deprecation warnings enabled surfaces the `utcfromtimestamp` deprecation (Python 3.12+) pointing at that exact line:

```bash
python3 -W all::DeprecationWarning -c "import scapy"
```

```text
/tmp/blitzy/scapy/blitzy-fe4437a6-27e6-4b3a-ac0f-97db5358536b_a9e597/scapy/__init__.py:160: DeprecationWarning: datetime.datetime.utcfromtimestamp() is deprecated and scheduled for removal in a future version. Use timezone-aware objects to represent datetimes in UTC: datetime.datetime.fromtimestamp(timestamp, datetime.UTC).
  d = datetime.datetime.utcfromtimestamp(os.path.getmtime(__file__))
```

**Emphasis:** `2026.07.06` is a **computed date artifact — the modification date of `scapy/__init__.py` — not a released version tag.** (The mtime epoch above, `1783375852…`, is specific to *this* checkout's file; a different checkout may show a different epoch but the same date, hence the same version string.)

### Sources (web validation)

Scapy's public release stream is git‑tag‑derived — PyPI states "Scapy supports Python 3.7+." and the docs are published under tag‑style versions such as `2.7.0`/`2.7.1`. The `_version_from_git_describe()` helper uses a `git describe`‑based scheme (visible in the project's own history, e.g. GitHub issue #1029). Together these corroborate that a bare *date‑form* value like `2026.07.06` is the **fallback**, not a tag. URLs: `https://pypi.org/project/scapy/`, `https://scapy.readthedocs.io/`.


---

## Q3 — Create a basic ICMP echo request; what IP‑header fields get auto‑populated?

**Direct answer:** for `IP(dst="127.0.0.1")/ICMP()`, four IP fields are **auto‑populated** — `ihl`, `len`, `chksum`, and `src` — meaning they are stored as `None` before assembly and computed *during* assembly. The rest carry **static declared defaults** from `fields_desc`: `version=4`, `tos=0`, `id=1`, `flags=0`, `frag=0`, `ttl=64`, `proto` (default `0`, **overloaded to `icmp`=1** by the ICMP layer), and `dst`.

**Command (run from the checkout root so `scapy` imports from the checkout):**

```bash
python3 -c "from scapy.all import IP, ICMP; p = IP(dst='127.0.0.1')/ICMP(); \
print(bytes(p).hex()); print(len(bytes(p)))"
```

**Stored (pre‑build) field values** — `None` for exactly the four auto‑populated fields:

```text
getfieldval('version' ) = 4
getfieldval('ihl'     ) = None
getfieldval('tos'     ) = 0
getfieldval('len'     ) = None
getfieldval('id'      ) = 1
getfieldval('flags'   ) = <Flag 0 ()>
getfieldval('frag'    ) = 0
getfieldval('ttl'     ) = 64
getfieldval('proto'   ) = 1
getfieldval('chksum'  ) = None
getfieldval('src'     ) = None
getfieldval('dst'     ) = '127.0.0.1'
```

**Assembled wire bytes** (deterministic — this reproduces exactly):

```text
bytes(p).hex() = 4500001c0001000040017cde7f0000017f0000010800f7ff00000000
len(bytes(p))  = 28
```

**Post‑build values, recovered by re‑dissecting `IP(bytes(p))`** — the previously‑`None` fields are now filled:

```text
version    = 4
ihl        = 5
tos        = 0
len        = 28
id         = 1
flags      = <Flag 0 ()>
frag       = 0
ttl        = 64
proto      = 1
chksum     = 0x7cde
src        = '127.0.0.1'
dst        = '127.0.0.1'
ICMP.type   = 8
ICMP.chksum = 0xf7ff
IP.chksum   = 0x7cde
```

**Field‑by‑field classification with citations** (all from the `IP.fields_desc` list, `scapy/layers/inet.py:L524-L537`):

| Field | Kind | Declared default | Source |
|-------|------|------------------|--------|
| `version` | static | `4` | `BitField("version", 4, 4)` — `inet.py:L524` |
| `ihl` | **auto** | `None` | `BitField("ihl", None, 4)` — `inet.py:L525` |
| `tos` | static | `0` | `XByteField("tos", 0)` — `inet.py:L526` |
| `len` | **auto** | `None` | `ShortField("len", None)` — `inet.py:L527` |
| `id` | static | `1` | `ShortField("id", 1)` — `inet.py:L528` |
| `flags` | static | `0` | `FlagsField("flags", 0, 3, ["MF","DF","evil"])` — `inet.py:L529` |
| `frag` | static | `0` | `BitField("frag", 0, 13)` — `inet.py:L530` |
| `ttl` | static | `64` | `ByteField("ttl", 64)` — `inet.py:L531` |
| `proto` | static (overloaded) | `0` → `1` | `ByteEnumField("proto", 0, IP_PROTOS)` — `inet.py:L532` |
| `chksum` | **auto** | `None` | `XShortField("chksum", None)` — `inet.py:L533` |
| `src` | **auto** | `None` (routed) | `SourceIPField("src", "dst")` — `inet.py:L535` |
| `dst` | static | `"127.0.0.1"` here | `DestIPField("dst", "127.0.0.1")` — `inet.py:L536` |

**Why `proto` already reads `1` before build:** the `/` composition applies ICMP's overload. `bind_layers(IP, ICMP, frag=0, proto=1)` (`scapy/layers/inet.py:L1112`) populates ICMP's overload map, verified at runtime:

```text
ICMP().overload_fields = {<class 'scapy.layers.inet.IP'>: {'frag': 0, 'proto': 1}}
```

so stacking ICMP onto IP sets the IP `proto` to `1` (which `show()` renders via the enum as `icmp`) and `frag` to `0`. The ICMP `type` defaults to echo‑request (`8`) via `ByteEnumField("type", 8, icmptypes)` (`scapy/layers/inet.py:L954`; `class ICMP` at `scapy/layers/inet.py:L952`).

**Byte‑level confirmation** of the four auto‑populated fields in `4500001c0001000040017cde…`: `45` = version 4 / **ihl 5**; `001c` = **len 28**; `7cde` = **IP chksum 0x7cde**; `7f000001` (bytes 12–15) = **src 127.0.0.1** (the routed value); and in the ICMP portion `0800f7ff`, `f7ff` = **ICMP chksum 0xf7ff**.


---

## Q4 — What does `show()` look like?

**Command:**

```bash
python3 -c "from scapy.all import IP, ICMP; (IP(dst='127.0.0.1')/ICMP()).show()"
```

**Output (verbatim — note the trailing space after `###[ IP ]###` and `###[ ICMP ]###`):**

```text
###[ IP ]### 
  version   = 4
  ihl       = None
  tos       = 0x0
  len       = None
  id        = 1
  flags     = 
  frag      = 0
  ttl       = 64
  proto     = icmp
  chksum    = None
  src       = 127.0.0.1
  dst       = 127.0.0.1
  \options   \
###[ ICMP ]### 
     type      = echo-request
     code      = 0
     chksum    = None
     id        = 0x0
     seq       = 0x0
     unused    = ''
```

The rendering is produced by `Packet.show()` (`scapy/packet.py:L1459`).

**Critical nuance — `show()` *displays* a `src` value that is not actually *stored* yet.** Notice `show()` prints `src = 127.0.0.1` while `ihl`, `len`, and `chksum` print as `None`. That is not inconsistent: `show()` asks each field for a display value, and `SourceIPField` resolves `src` from the destination route for display, but the stored field value is still `None` until assembly. Proven on the same object:

```bash
python3 -c "from scapy.all import IP, ICMP; p = IP(dst='127.0.0.1')/ICMP(); \
print('getfieldval:', repr(p[IP].getfieldval('src'))); \
print('attribute  :', repr(p[IP].src))"
```

```text
getfieldval: None          # STORED value
attribute  : '127.0.0.1'   # DISPLAYED / resolved value
```

- `p[IP].getfieldval("src")` → **`None`** — the raw stored value.
- `p[IP].src` (attribute access) → `'127.0.0.1'` — resolved for display.

`src` is a `SourceIPField` (`scapy/fields.py:L854`, a subclass of `IPField` at `scapy/fields.py:L796`) whose default is `None`; it resolves the address through `IP.route()` (see Q6). The materialized stored value is only produced when the packet is assembled via `Packet.build()` (`scapy/packet.py:L746`) → `Packet.post_build()` (`scapy/packet.py:L758`), which for the IP layer dispatches to `IP.post_build()`. The same reasoning explains why `ihl`, `len`, and `chksum` render as `None` pre‑build — they have no display‑time resolver, so their stored `None` shows through until `IP.post_build()` computes them.


---

## Q5 — Send a crafted ICMP packet to localhost; describe the network‑layer behavior

**Direct answer:** with Scapy's **default** layer‑3 socket (`L3PacketSocket`, PF_PACKET), sending to `127.0.0.1` gets **no answer** — `sr1()` returns `None`. Switching to `L3RawSocket` (PF_INET/SOCK_RAW) makes the same send receive the loopback **echo‑reply**. This is *documented* Scapy behavior for the loopback interface, not an environment defect.

**Route resolution first:**

```bash
python3 -c "from scapy.all import conf; print(conf.route.route('127.0.0.1'))"
```

```text
('lo', '127.0.0.1', '0.0.0.0')
```

i.e. interface `lo`, source `127.0.0.1`, gateway `0.0.0.0` — resolved by `Route.route()` (`scapy/route.py:L146`).

### Q5a — Default `L3PacketSocket` (Linux default)

`conf.L3socket` defaults to `L3PacketSocket` on Linux (`scapy/config.py:L647`):

```bash
python3 -c "from scapy.all import conf; print(conf.L3socket)"
# <L3PacketSocket: read/write packets at layer 3 using Linux PF_PACKET sockets>

python3 -c "from scapy.all import IP, ICMP, sr1; print(repr(sr1(IP(dst='127.0.0.1')/ICMP(), timeout=3)))"
```

**Output:**

```text
Begin emission:
Finished sending 1 packets.

Received 1 packets, got 0 answers, remaining 1 packets
RESULT: None
```

⇒ **`sr1()` returns `None` — the loopback echo‑reply is *not* matched.** This is the truthful primary result and is reported as observed.

### Q5b — `L3RawSocket` (the remedy)

```bash
python3 -c "from scapy.all import IP, ICMP, sr1, conf; \
from scapy.supersocket import L3RawSocket; \
conf.L3socket = L3RawSocket; \
p = sr1(IP(dst='127.0.0.1')/ICMP(), timeout=3); p.show()"
```

**Output:**

```text
conf.L3socket set to: <L3RawSocket: Layer 3 using Raw sockets (PF_INET/SOCK_RAW)>
Begin emission:
Finished sending 1 packets.

Received 2 packets, got 1 answers, remaining 0 packets
###[ IP ]### 
  version   = 4
  ihl       = 5
  tos       = 0x0
  len       = 28
  id        = 56468
  flags     = 
  frag      = 0
  ttl       = 64
  proto     = icmp
  chksum    = 0xa04a
  src       = 127.0.0.1
  dst       = 127.0.0.1
  \options   \
###[ ICMP ]### 
     type      = echo-reply
     code      = 0
     chksum    = 0x0
     id        = 0x0
     seq       = 0x0
     unused    = ''
```

⇒ now `got 1 answers`, and the reply is an `IP/ICMP` **`echo-reply`** (ICMP `type=0`).

> **Nuance — the reply's `IP.id` and checksums are kernel‑assigned and vary per run.** My run shows `IP.id=56468`, `IP.chksum=0xa04a`, `ICMP.chksum=0x0`; a subsequent run will differ. Only these variable fields change — the *behavior* (0 answers on the default socket, 1 answer on `L3RawSocket`) is stable.

**Causal explanation (call path + why):** `sr1()` (`scapy/sendrecv.py:L656`) drives `sndrcv()` (`scapy/sendrecv.py:L322`); the outgoing interface is chosen by `_interface_selection()` (`scapy/sendrecv.py:L616`) from the route above; the low‑level transmit is `send()` (`scapy/sendrecv.py:L422`). The difference is the socket class:

- The default **`L3PacketSocket`** (`scapy/config.py:L647`) uses an **AF_PACKET** socket, which injects *below* the IP stack. On loopback the kernel routes the packet while it is still inside an internal structure, so the "reply" is never delivered back up to Scapy's socket for matching — hence 0 answers.
- **`L3RawSocket`** (`scapy/config.py:L672`) uses a **PF_INET/SOCK_RAW** socket — one layer higher — so the echo‑reply is received and matched.

### Sources (web validation)

Scapy's official troubleshooting documentation documents exactly this limitation and the remedy. The FAQ lists the item "Scapy does not work with 127.0.0.1 or on the loopback interface," and the fix is to set `conf.L3socket = L3RawSocket` to build packets "one layer upper" (PF_INET/SOCK_RAW instead of PF_PACKET). The docs also note that "Scapy will automatically use L3RawSocket when necessary" on loopback for versions > 2.5.0 when libpcap is not in use — yet in *this* checkout the default `sr1()` still returned 0 answers, so the manual remedy was required; I report the observed truth (the auto‑switch did not apply for this call). URLs: `https://scapy.readthedocs.io/en/latest/troubleshooting.html`, `https://github.com/secdev/scapy/blob/master/doc/scapy/troubleshooting.rst`.


---

## Q6 — Explore the source: how does Scapy construct the IP header by default?

Default IP construction is a **two‑stage** mechanism — declarative defaults at class‑definition time, then deferred computation at assembly time — plus routed `src` resolution.

### 1. Declarative defaults (`fields_desc`)

`class IP(Packet, IPTools)` (`scapy/layers/inet.py:L521`) declares its header as an ordered list of field objects, each constructed with a default value (`scapy/layers/inet.py:L524-L537`). This is the same list enumerated in Q3. The key convention: **a `None` default is the signal "compute this at build time."** So `ihl` (`L525`), `len` (`L527`), and `chksum` (`L533`) are declared `None`, and `src` is a `SourceIPField` (`L535`) whose base default is also `None`.

### 2. Deferred computation (`IP.post_build`)

When the packet is assembled (`bytes(p)` → `Packet.build()` at `scapy/packet.py:L746` → `Packet.post_build()` at `scapy/packet.py:L758`), the IP layer's override `IP.post_build(self, p, pay)` (`scapy/layers/inet.py:L539-L551`) fills the `None` fields. Verbatim source:

```python
def post_build(self, p, pay):
    ihl = self.ihl
    p += b"\0" * ((-len(p)) % 4)  # pad IP options if needed
    if ihl is None:
        ihl = len(p) // 4
        p = chb(((self.version & 0xf) << 4) | ihl & 0x0f) + p[1:]
    if self.len is None:
        tmp_len = len(p) + len(pay)
        p = p[:2] + struct.pack("!H", tmp_len) + p[4:]
    if self.chksum is None:
        ck = checksum(p)
        p = p[:10] + chb(ck >> 8) + chb(ck & 0xff) + p[12:]
    return p + pay
```

- pads the options to a 4‑byte boundary;
- if `ihl is None` → `ihl = len(p)//4` and rewrites byte 0 (`inet.py:L542-L544`);
- if `len is None` → writes the total length with `struct.pack("!H", …)` (`inet.py:L545-L547`);
- if `chksum is None` → computes `checksum(p)` and writes it (`inet.py:L548-L550`);
- returns `p + pay` (`inet.py:L551`).

### 3. `src` resolution via routing

The `SourceIPField` default triggers routing through `IP.route()` (`scapy/layers/inet.py:L559-L566`):

```python
def route(self):
    dst = self.dst
    if isinstance(dst, Gen):
        dst = next(iter(dst))
    if conf.route is None:
        # unused import, only to initialize conf.route
        import scapy.route  # noqa: F401
    return conf.route.route(dst)   # inet.py:L566
```

which delegates to `conf.route.route()` (`scapy/route.py:L146`); for `dst=127.0.0.1` that returns the `lo` source `127.0.0.1` (the same tuple seen in Q5).

### Runtime confirmation of the "`None` means compute" contract

The `if <field> is None:` guards mean a user‑supplied value is honored and *not* recomputed. Demonstrated at runtime:

```bash
python3 -c "
from scapy.all import IP, ICMP
# A: chksum defaults to None -> computed
print('A computed IP chksum:', hex(IP(bytes(IP(dst='127.0.0.1')/ICMP())).chksum))
# B: force chksum=0 -> honored, NOT recomputed
print('B assembled IP chksum:', hex(IP(bytes(IP(dst='127.0.0.1', chksum=0)/ICMP())).chksum))
# C: force ihl/len -> honored
q = IP(bytes(IP(dst='127.0.0.1', ihl=15, len=99)/ICMP()))
print('C assembled ihl/len:', q.ihl, q.len)
"
```

```text
A computed IP chksum : 0x7cde
B assembled IP chksum: 0x0    (user value honored, NOT recomputed)
C assembled ihl/len  : 15 99  (user values honored)
```

**Tie‑back to Q3/Q4:** the `None`‑valued declarative defaults (`ihl`, `len`, `chksum`) plus the routed `src` are exactly the fields that show as `None` before assembly and get filled by `IP.post_build()` (and, for `src`, by `IP.route()`) at build time. That is why Q4's `show()` displays a resolved `src` while its stored value — and the other three — remain `None` until `bytes(p)` is taken.


---

## Q7 — Run the test suite: how many tests, and what is the pass/fail summary?

**Direct answer (measured, stable across two runs):** the Linux campaign contains **190 campaigns / 5017 tests**, with **PASSED=4678, FAILED=339** (exit code `1`). All 339 failures are **environmental** (missing optional dependencies, a removed `cryptography` module, no network, etc.) — not code defects.

**Harness:** Scapy uses its **own UTScapy harness** (not pytest). The flow is `main()` (`scapy/tools/UTscapy.py:L966`) → `parse_config_file()` (`scapy/tools/UTscapy.py:L307`) → `execute_campaign()` (`scapy/tools/UTscapy.py:L882`) → `run_campaign()` (`scapy/tools/UTscapy.py:L575`); each campaign prints a `PASSED=%i FAILED=%i` summary (`scapy/tools/UTscapy.py:L619`), and the harness ends with `UTscapy ended with error code 1` (`scapy/tools/UTscapy.py:L1221`) whenever any test fails.

**Canonical command** (per `tox.ini:L42-L46`; env `linux_non_root` adds `-N`):

```bash
python3 -m scapy.tools.UTscapy -c ./test/configs/linux.utsc -N -b -o /tmp/utscapy_report.html
```

**Flag rationale (each matters):**

- `-c ./test/configs/linux.utsc` — the Linux campaign config. It globs `test/*.uts`, `test/scapy/layers/*.uts`, `test/contrib/*.uts`, `test/tools/*.uts`, the enumerated automotive subdirs (`obd`, `scanner`, `gm`, `bmw`, `xcp`, `autosar`), and `test/tls/tests_tls_netaccess.uts`; **removes** `test/windows.uts` and `test/bpf.uts`; sets `breakfailed=true` and `onlyfailed=true`; and skips the keywords `osx`, `windows`, `ipv6`.
- `-b` — **must be present, and must come after `-c`.** Because `linux.utsc` sets `breakfailed=true`, the plain command stops at the first failing campaign; `-b` sets `BREAKFAILED = False` (`scapy/tools/UTscapy.py:L1002-L1003`) so the full suite runs. `-c` loads the config that sets `breakfailed`, so `-b` has to follow it to override.
- `-N` — non‑root mode (I confirmed the header prints `Non-root mode`).
- `-o /tmp/utscapy_report.html` — diverts the default HTML report (otherwise `OUTPUTFILE = sys.stdout`, `scapy/tools/UTscapy.py:L979`) so it does not flood stdout.

**Harness header (confirms version + interpreter + mode):**

```text
━ UTScapy - Scapy 2026.07.06 - 3.13.7
 └ Loaded config file ./test/configs/linux.utsc
 └ Non-root mode
 └ Booting scapy...
 └ Discovering tests files...
━ Loading: test/tls/tests_tls_netaccess.uts
Campaign CRC=BD72757A in 000.00s SHA=8C102D92251C58A841DCE5EFE3F810AD9038E69C
PASSED=0 FAILED=0
```

**Measured results — two identical runs ⇒ stable:**

| Run | Campaigns | PASSED | FAILED | TOTAL | Exit | Duration |
|-----|-----------|--------|--------|-------|------|----------|
| 1 | 190 | 4678 | 339 | **5017** | 1 | ~100 s |
| 2 | 190 | 4678 | 339 | **5017** | 1 | ~99 s |

The tail of each run ends with:

```text
✓ All campaigns executed. Writing output...
UTscapy ended with error code 1
```

The exit code `1` is **expected** whenever any test fails (`scapy/tools/UTscapy.py:L1221`); it does not indicate a harness malfunction.

**Failure breakdown (identical across both runs — 20 campaigns, 339 failures):**

| Failures | Campaign |
|---------:|----------|
| 67 | `test/regression.uts` |
| 62 | `test/cert.uts` |
| 60 | `test/tls.uts` |
| 45 | `test/tls13.uts` |
| 28 | `test/contrib/isotp_native_socket.uts` |
| 26 | `test/sslv2.uts` |
| 15 | `test/contrib/isotpscan.uts` |
| 9 | `test/scapy/layers/inet6.uts` |
| 7 | `test/answering_machines.uts` |
| 5 | `test/pipetool.uts` |
| 3 | `test/scapy/layers/l2.uts` |
| 2 | `test/linux.uts` |
| 2 | `test/scapy/layers/inet.uts` |
| 2 | `test/contrib/gxrp.uts` |
| 1 each | `test/imports.uts`, `test/p0fv2.uts`, `test/scapy/layers/dhcp.uts`, `test/scapy/layers/http.uts`, `test/contrib/dtp.uts`, `test/contrib/automotive/scanner/uds_scanner.uts` |

**Root causes — all environmental, not code defects** (extracted from the HTML report):

```text
  66  ModuleNotFoundError: No module named 'mock'
  46  ModuleNotFoundError: No module named 'cryptography.hazmat.backends.openssl.ec'
  26  name 'exit_if_no_isotp_module' is not defined
  18  ImportError: The mock module needs to be installed !
  15  name 'TLS' is not defined      (and 'SSLv2'/'TLS13'/'Cert' not defined)
  12  name 'ISOTPSoftSocket' is not defined
   7  Cannot find libpcap.so library
   4  Could not execute tshark, is it installed?
```

- **The TLS block** (`cert.uts` 62 + `tls.uts` 60 + `tls13.uts` 45 + `sslv2.uts` 26 = 193 failures) is caused by **`cryptography 43.0.0` removing `cryptography.hazmat.backends.openssl.ec`**, which Scapy's TLS layer imports at `scapy/layers/tls/cert.py:L51` (`from cryptography.hazmat.backends.openssl.ec import InvalidSignature`) and references in `scapy/layers/tls/crypto/cipher_block.py:L23`. The failed import prevents the TLS layer from loading, so those campaigns cascade into `name 'TLS'/'SSLv2'/'TLS13'/'Cert' is not defined`. Confirmed: `python3 -c "import cryptography.hazmat.backends.openssl.ec"` raises `ModuleNotFoundError`.
- **`regression.uts` (67)** and many `mock` errors are due to the **`mock` module and matplotlib being absent**.
- **ISO‑TP campaigns (`isotp_native_socket.uts` 28, `isotpscan.uts` 15)** are due to **python‑can / vcan being absent**.
- The remaining handful stem from **no libpcap**, **no tshark/wireshark**, IPv6 link‑local specifics, and routing‑table interface naming.

> **Why my numbers differ from a "clean" run.** The TOTAL (5017) and campaign count (190) are the magnitude of the suite. My **PASSED/FAILED split (4678/339)** is higher on failures than an environment with an older `cryptography` (e.g. 41.0.7 still ships `openssl.ec`, so the ~193 TLS tests would pass there). The delta is entirely attributable to the environmental factors above; the suite magnitude itself is stable.

**Cleanup:** each run wrote a `RMBA_dump.hex` (53 bytes) in the repo root as a hexdump test side‑effect; it was deleted after every run, and the HTML reports were written under `/tmp`.

---

## Read‑only integrity

This was a read‑only investigation. To honor that:

- **No source file was modified.** Every Scapy source and config file above was consulted for citation only.
- **Temporary observation scripts** lived under `/tmp` (e.g. `/tmp/trace_version.py`) and were removed afterward.
- **`RMBA_dump.hex`** — the hexdump side‑effect a test run leaves in the repo root — was deleted after each Q7 run; the UTScapy HTML reports were written to `/tmp`.
- **Final check:** `git status --porcelain --untracked-files=all` shows only this new file:

  ```text
  ?? blitzy/documentation/scapy_0925ada48540.md
  ```

The only change to the repository is the addition of this document.

