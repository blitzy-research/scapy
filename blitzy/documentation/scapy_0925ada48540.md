# What Scapy Reveals as a Packet Moves Through Build → Send → Sniff

## Introduction

This document answers a single question by **observation, not by reading alone**: when you
start Scapy in its normal configuration, build a simple multi-layer packet (an
`Ether()/IP()/TCP()` stack), send it, and then sniff for that same traffic, **what does
Scapy actually show you at each stage, and why?**

Everything below was produced by **running Scapy from its own source tree** and capturing the
real output. The direct answer is stated plainly for each item, backed by the **exact
observed output** and a **`file:line` citation** into the source, with cause→effect reasoning.

**Investigation baseline (all values observed at runtime):**

| Fact | Observed value |
|------|----------------|
| Repository commit | `0925ada485406684174d6f068dbd85c4154657b3` |
| Python interpreter | **3.13.7** (satisfies `pyproject.toml` `requires-python >=3.7,<4`) |
| Privileges | **root (uid 0)** — required for raw-socket send/sniff |
| Scapy version | **`scapy.VERSION = 2026.07.14`** (run from source, not an installed release) |
| Default verbosity | **`conf.verb = 2`** |
| Default interface | **`conf.iface = eth0`** |
| Interfaces present | `lo` (127.0.0.1), `eth0` (10.236.0.248), `docker0` (172.17.0.1) |
| Color theme (script) | `NoTheme` (non-interactive → no ANSI codes) |

**Canonical entry points used.** The interactive console `python3 -m scapy` (wired as the
`scapy` console script through `scapy/main.py`), and library use via `import scapy.all`. No
debug hooks, mocks, or synthetic byte stand-ins were used — every observation goes through the
real public API (`repr`, `show`, `send`, `sendp`, `sniff`, `AsyncSniffer`).

**A note on color, stated once.** All packet output below is shown **ANSI-stripped** for
readability. In a **non-interactive** script (piped stdout) `conf.color_theme` is `NoTheme`,
so Scapy emits plain text with no escape codes. The **interactive console** installs a
**colored** theme, so the raw banner and packet dumps there contain ANSI color codes; stripping
them yields exactly the text shown here.

**What is deterministic here, and what varies.** Every observation below was produced from **this
exact checkout** (`0925ada`), on **this host and OS**, with **this Python and package set**, for
**this exact `Ether()/IP()/TCP()` packet**. Scoped that way, the *structural* results reproduce
run-to-run: the field reprs, `summary`/`command`, the `show()`↔`show2()` contrast, the 54-byte
length and the checksums `0x7ccd` (IP) / `0x917c` (TCP), the `Sent N packets.` reporting, and the
dissected layer structure. **Many surface values are environment- or run-dependent, however, and
must not be read as universal** (each is flagged where it appears):

- the **rotating banner tagline** — a random quote, different on every launch;
- **host-specific routing** — interface names, output IPs, and gateway IPs (`10.236.0.*` here);
- the **Python version** (`3.13.7` here) and **which optional packages are present** — IPython and
  `libpcap` absent, `cryptography` present — which change the banner, the shell fallback, and
  whether BPF filtering compiles;
- **absolute file paths** in warnings (this repo's checkout path);
- the **socket backend** — native Linux `AF_PACKET` here; other OSes select different sockets;
- **service names** such as `ftp_data`/`http`, resolved from the system services database;
- the **exact bytes and checksums**, which depend entirely on the packet's field values (a
  different `dport`, `src`, etc. changes the checksums and the hexdump);
- the **capture multiplicity** — the loopback double-capture is specific to loopback;
- and, on a busy host, **which frames match** the correlation predicate.

---

## 1. Starting Scapy — Environment and Version

**Direct answer.** Launching the console with `python3 -m scapy` prints an informational
banner that ends with the canonical version line **`Version 2026.07.14`** and drops you at a
`>>>` prompt. Because **IPython is absent** in this environment, Scapy falls back to the
standard Python shell and says so. Because **libpcap is absent**, BPF filter compilation will
later fail — but native Linux capture still works (shown in §4).

The banner is produced by the console entry point (`scapy/main.py` `interact`), captured with
`echo "exit()" | python3 -m scapy`. Scapy splits its startup across **two streams**, shown
**separately** below: **stdout** carries only the interactive prompt, while the **entire banner**
— the informational lines, the two deprecation warnings, the `WARNING`/`AutoCompletion` notice,
and the ASCII art — is written to **stderr**.

**stdout — exactly 17 bytes: the ANSI-colored prompt and nothing else** (shown with the escape
bytes visible):

```
\x1b[34m\x1b[1m>>> \x1b[0m
```

That is the standard Python REPL prompt — `>>> ` (three `>` and one trailing space, no trailing
newline) — wrapped in Scapy's prompt-color escapes (`ESC[34m` blue, `ESC[1m` bold, `ESC[0m`
reset). ANSI-stripped it is simply `>>> `. It lands on stdout because the fallback standard
Python shell prints its prompt there; every informational line goes to **stderr** instead.

**stderr — the complete banner, ANSI-stripped and unedited** (the deprecation-warning paths are
the real absolute repository paths, not elided; the `| ...` tagline is a rotating quote that
differs on every launch — see the note directly after the banner):

```
INFO: Can't import PyX. Won't be able to use psdump() or pdfdump().
INFO: No IPv6 support in kernel
/tmp/blitzy/scapy/blitzy-25029afa-3880-4f96-a5df-88edf3ddbb3d_d58022/scapy/layers/ipsec.py:573: CryptographyDeprecationWarning: TripleDES has been moved to cryptography.hazmat.decrepit.ciphers.algorithms.TripleDES and will be removed from this module in 48.0.0.
  cipher=algorithms.TripleDES,
/tmp/blitzy/scapy/blitzy-25029afa-3880-4f96-a5df-88edf3ddbb3d_d58022/scapy/layers/ipsec.py:577: CryptographyDeprecationWarning: TripleDES has been moved to cryptography.hazmat.decrepit.ciphers.algorithms.TripleDES and will be removed from this module in 48.0.0.
  cipher=algorithms.TripleDES,
WARNING: IPython not available. Using standard Python shell instead.
AutoCompletion, History are disabled.
                                      
                     aSPY//YASa       
             apyyyyCY//////////YCa       |
            sY//////YSpcs  scpCY//Pp     | Welcome to Scapy
 ayp ayyyyyyySCP//Pp           syY//C    | Version 2026.07.14
 AYAsAYYYYYYYY///Ps              cY//S   |
         pCCCCY//p          cSSps y//Y   | https://github.com/secdev/scapy
         SPPPP///a          pP///AC//Y   |
              A//A            cyP////C   | Have fun!
              p///Ac            sC///a   |
              P////YCpc           A//A   | To craft a packet, you have to be a
       scccccp///pSP///p          p//Y   | packet, and learn how to swim in
      sY/////////y  caa           S//P   | the wires and in the waves.
       cayCyayP//Ya              pY/Ya   |        -- Jean-Claude Van Damme
        sY/PsY////YCc          aC//Yp    |
         sc  sccaCY//PCypaapyCP//YSs  
                  spCPY//////YPSps    
                       ccaacs         
                                      
now exiting InteractiveConsole...
```

**What each part means (observed → cause):**

- **`Version 2026.07.14`** — the canonical version string, equal to `scapy.VERSION`. Confirmed
  independently from a library import: `import scapy; scapy.VERSION` → `2026.07.14`.
- **`WARNING: IPython not available. Using standard Python shell instead. AutoCompletion,
  History are disabled.`** — the direct, observed evidence that IPython is not installed here,
  so the console uses the plain Python REPL.
- **`INFO: No IPv6 support in kernel`** and **`INFO: Can't import PyX ...`** — optional
  subsystems that are simply unavailable; informational, not errors.
- **`CryptographyDeprecationWarning: TripleDES ...`** — a **pre-existing Scapy/`cryptography`
  compatibility deprecation warning**, unrelated to this investigation. It is emitted by the
  `cryptography` library (which *is* present) at the moment Scapy imports `scapy/layers/ipsec.py`,
  where the 3DES cipher is referenced [scapy/layers/ipsec.py:L573, scapy/layers/ipsec.py:L577].
  A newer `cryptography` has relocated `TripleDES` to a `decrepit` submodule, so the warning
  reflects version skew between this Scapy checkout and the installed `cryptography` 43.0.0; it
  is harmless here and changes none of the packet output below.
- **The final `| ...` quote line varies run-to-run** — it is a random rotating tagline. On a
  second launch the same slot showed `Craft me if you can. -- IPv6 layer` instead of the
  Jean-Claude Van Damme quote above. Do not treat it as fixed.

**Default configuration a normal user sees.** From `import scapy.all`, the defaults that govern
everything below are `conf.verb = 2` (verbosity), `conf.iface = eth0` (default send interface),
and `conf.color_theme = NoTheme` in a piped script. On Linux, Scapy selects native `AF_PACKET`
sockets for I/O — `conf.L3socket = L3PacketSocket` [scapy/config.py:L647], `conf.L2socket =
L2Socket` [scapy/config.py:L655], and `conf.L2listen = L2ListenSocket` [scapy/config.py:L656] —
observed as `L3PacketSocket`, `L2Socket`, and `L2ListenSocket` respectively. (Importing only
`scapy.config` leaves these `None`; `import scapy.all` performs the full architecture init.)

---

## 2. Building `Ether()/IP()/TCP()` — What Scapy Shows as Each Layer Is Added

### 2.1 The `/` operator stacks layers

**Direct answer.** The `/` operator composes layers: `Ether()/IP()/TCP()` builds a three-layer
packet where `Ether` carries `IP` as its payload, and `IP` carries `TCP`. `/` is implemented by
`Packet.__div__` [scapy/packet.py:L596] (`__truediv__ = __div__` [scapy/packet.py:L607]); it
clones both operands and attaches the right operand as the left operand's payload via
`add_payload()` [scapy/packet.py:L360].

### 2.2 Selective `__repr__`: only set or overloaded, non-empty fields appear

**Direct answer.** The interactive `repr` shows a layer's fields **selectively** — only fields
the user set, or fields *overloaded* by a protocol binding, **and even then only when the value
is non-empty and any conditional guard is satisfied**. Everything left at its plain default (and
every empty collection) is omitted. Observed, building up one layer at a time:

```
repr(Ether())            -> <Ether  |>
repr(Ether()/IP())       -> <Ether  type=IPv4 |<IP  |>>
repr(Ether()/IP()/TCP()) -> <Ether  type=IPv4 |<IP  frag=0 proto=tcp |<TCP  |>>>
```

**Cause → effect.** `Packet.__repr__` [scapy/packet.py:L552] walks the field descriptors
[scapy/packet.py:L556] and prints a field **only if** it clears three guards. (1) A
`ConditionalField` whose condition is currently false is skipped [scapy/packet.py:L557-558].
(2) The field must appear in `self.fields` (explicitly set) [scapy/packet.py:L559] **or** in
`self.overloaded_fields` (binding-driven) [scapy/packet.py:L564]; a field in neither hits the
`else: continue` branch [scapy/packet.py:L569] and is skipped. (3) Even when a field *is* set or
overloaded, if its value is an **empty** `list`/`dict`/`set` it is still skipped
[scapy/packet.py:L561-562] — the identical guard also applies on the overloaded branch
[scapy/packet.py:L566-567]. The survivors are wrapped as `<LayerName field=val |payload>` at
[scapy/packet.py:L581].

Guard (3) is exactly why the observed `eit[IP].fields` below is `{'options': []}` yet the IP
`repr` shows **no** `options` field: the field *is* set, but its value is an empty list, so the
empty-collection guard drops it. `repr` is therefore deliberately terse — for the **complete**
field view (including empty collections and the auto-computed fields) use `show()`/`show2()`
in §2.4.

- **`Ether()` alone shows no fields** because every field is a plain default. Its `type` default
  is `0x9000` (observed: `Ether().get_field("type").default` → `0x9000`), which is neither set
  nor overloaded, so it is skipped.
- **Stacking overloads fields.** The overloads come from `bind_layers` declarations. Observed on
  the composite packet `eit = Ether()/IP()/TCP()`:

```
eit[Ether].overloaded_fields = {'type': 2048}
eit[Ether].fields            = {}
eit[IP].overloaded_fields    = {'frag': 0, 'proto': 6}
eit[IP].fields               = {'options': []}
eit[TCP].overloaded_fields   = {}
eit[TCP].fields              = {}
```

So `type=IPv4` (2048 = `0x0800`) appears once `IP` is stacked under `Ether`, and `frag=0` and
`proto=tcp` (6) appear once `TCP` is stacked under `IP` — even though the user never set them.
These overloads are registered by `bind_layers(Ether, IP, type=2048)`
[scapy/layers/inet.py:L1101] and `bind_layers(IP, TCP, frag=0, proto=6)`
[scapy/layers/inet.py:L1113]. The layer classes themselves are `Ether`
[scapy/layers/l2.py:L244], `IP` [scapy/layers/inet.py:L521], and `TCP`
[scapy/layers/inet.py:L753].

### 2.3 `summary()` and `command()`

**Direct answer.** `summary()` gives a one-line human description; `command()` reconstructs the
Python constructor call that would rebuild the packet. Observed:

```
(Ether()/IP()/TCP()).summary() -> 'Ether / IP / TCP 127.0.0.1:ftp_data > 127.0.0.1:http S'
(Ether()/IP()/TCP()).command() -> 'Ether()/IP()/TCP()'
```

`summary()` [scapy/packet.py:L1642] renders `Ether / IP / TCP` plus a protocol-specific tail;
the ports print as **service names** — `ftp_data` (20) for `sport` and `http` (80) for `dport`
— because they are enum fields, and `S` is the TCP **SYN** flag (the default `TCP.flags`).
`command()` [scapy/packet.py:L1662] rebuilds the constructor string from each layer's
`self.fields`; since no fields were explicitly set, it round-trips to exactly `Ether()/IP()/TCP()`.

### 2.4 `show()` (pre-build, in-memory) vs `show2()` (post-build, re-dissected)

**Direct answer.** `show()` prints the packet **as it currently sits in memory**, where
auto-computed fields (`IP.ihl/len/chksum`, `TCP.dataofs/chksum`) are still **`None`** because
they are only filled in when the packet is serialized. `show2()` first **builds the packet to
bytes and re-dissects it**, so those computed fields are now **resolved to real values**.

`show()` — computed fields are `None`:

```
###[ Ethernet ]### 
  dst       = ff:ff:ff:ff:ff:ff
  src       = 00:00:00:00:00:00
  type      = IPv4
###[ IP ]### 
     version   = 4
     ihl       = None
     tos       = 0x0
     len       = None
     id        = 1
     flags     = 
     frag      = 0
     ttl       = 64
     proto     = tcp
     chksum    = None
     src       = 127.0.0.1
     dst       = 127.0.0.1
     \options   \
###[ TCP ]### 
        sport     = ftp_data
        dport     = http
        seq       = 0
        ack       = 0
        dataofs   = None
        reserved  = 0
        flags     = S
        window    = 8192
        chksum    = None
        urgptr    = 0
        options   = ''
```

`show2()` — the **same packet** after `self.__class__(raw(self))` (build + re-dissect). This is the **complete, unedited** dump: every field is shown, and the fields that were `None` in `show()` above are now resolved (`IP.ihl/len/chksum`, `TCP.dataofs/chksum`), while all other fields are unchanged:

```
###[ Ethernet ]### 
  dst       = ff:ff:ff:ff:ff:ff
  src       = 00:00:00:00:00:00
  type      = IPv4
###[ IP ]### 
     version   = 4
     ihl       = 5
     tos       = 0x0
     len       = 40
     id        = 1
     flags     = 
     frag      = 0
     ttl       = 64
     proto     = tcp
     chksum    = 0x7ccd
     src       = 127.0.0.1
     dst       = 127.0.0.1
     \options   \
###[ TCP ]### 
        sport     = ftp_data
        dport     = http
        seq       = 0
        ack       = 0
        dataofs   = 5
        reserved  = 0
        flags     = S
        window    = 8192
        chksum    = 0x917c
        urgptr    = 0
        options   = ''
```

**Cause → effect.** `show()` [scapy/packet.py:L1459] prints the live in-memory object; the
auto-computed fields have not been calculated yet, so they read `None`. `show2()`
[scapy/packet.py:L1473] returns `self.__class__(raw(self)).show(...)`
[scapy/packet.py:L1486] — it serializes the packet through `raw()` [scapy/compat.py:L112],
which runs the build pipeline `build()` [scapy/packet.py:L746] → `do_build()`
[scapy/packet.py:L724] → `self_build()` [scapy/packet.py:L678] and then `post_build()`
[scapy/packet.py:L758], where `IP` and `TCP` override `post_build` to compute the length and
checksum. Re-parsing those bytes yields `ihl=5`, `len=40`, IP `chksum=0x7ccd`, TCP `dataofs=5`,
and TCP `chksum=0x917c`.

### 2.5 `hexdump()` — the assembled bytes before it leaves

**Direct answer.** The fully built packet is **54 bytes**; `hexdump()` shows them in the classic
tcpdump grid (offset, 16 hex bytes/row, ASCII column). Observed:

```
len(bytes(Ether()/IP()/TCP())) = 54
0000  FF FF FF FF FF FF 00 00 00 00 00 00 08 00 45 00  ..............E.
0010  00 28 00 01 00 00 40 06 7C CD 7F 00 00 01 7F 00  .(....@.|.......
0020  00 01 00 14 00 50 00 00 00 00 00 00 00 00 50 02  .....P........P.
0030  20 00 91 7C 00 00                                 ..|..
```

`hexdump()` is [scapy/utils.py:L273]. Reading the bytes against the layers confirms the build:
`08 00` is the Ethernet `type` (IPv4); `45` is IP `version=4`/`ihl=5`; `40 06` shows `ttl=64`
and `proto=6`; `7C CD` is the IP checksum (matching `show2()`'s `chksum=0x7ccd`); `7F 00 00 01`
twice is the loopback `127.0.0.1` source and destination; and the trailing `91 7C` is the TCP
checksum `0x917c`.

---


## 3. Sending — Routing, Interface Selection, and Transmission Reporting

### 3.1 The routing table Scapy consults

**Direct answer.** Before it can send, Scapy decides *which interface and source IP* to use by
consulting its own routing table, `conf.route`. Observed (`repr(conf.route)`, ANSI-stripped):

```
Network     Netmask          Gateway     Iface    Output IP     Metric
0.0.0.0     0.0.0.0          10.236.0.1  eth0     10.236.0.248  0     
10.236.0.0  255.255.255.0    10.236.0.1  eth0     10.236.0.248  0     
10.236.0.1  255.255.255.255  0.0.0.0     eth0     10.236.0.248  0     
127.0.0.0   255.0.0.0        0.0.0.0     lo       127.0.0.1     1     
172.17.0.0  255.255.0.0      0.0.0.0     docker0  172.17.0.1    0     
```

The six columns — **Network, Netmask, Gateway, Iface, Output IP, Metric** — are produced by
`Route.__repr__` [scapy/route.py:L47] (the header tuple is at [scapy/route.py:L60]). The `Route`
class itself is [scapy/route.py:L31]. The concrete IP addresses (`10.236.0.*`) are
**host-specific and will differ on another machine**; the structurally important row is
`127.0.0.0 / 255.0.0.0 → lo`, which is why a packet addressed to `127.0.0.1` is routed out the
loopback interface.

### 3.2 Route resolution: `(iface, output_ip, gateway_ip)`

**Direct answer.** Resolving a destination returns a 3-tuple `(iface, output_ip, gateway_ip)`.
For a loopback destination, both surfaces agree:

```
IP(dst="127.0.0.1").route()   -> ('lo', '127.0.0.1', '0.0.0.0')
conf.route.route("127.0.0.1") -> ('lo', '127.0.0.1', '0.0.0.0')
```

`IP.route()` [scapy/layers/inet.py:L559] delegates to `conf.route.route(dst)` (`return
conf.route.route(dst)` at [scapy/layers/inet.py:L566]), i.e. `Route.route()`
[scapy/route.py:L146], which returns the tuple and chooses the **most-specific** matching route
(greatest netmask, then metric). Both surfaces above are **manually invoked**: Scapy prints the
routing table and this tuple *only* when you evaluate `repr(conf.route)` / `.route()` yourself —
neither is emitted automatically when you call `send`/`sendp` (confirmed in §3.3, where the only
transmission output is the progress dots and the `Sent N packets.` line). At **layer 3**, route
resolution is what selects the outgoing interface: `_interface_selection()`
[scapy/sendrecv.py:L616] takes the first packet's route when no explicit interface is given —
`iff = next(packet.__iter__()).route()[0]` [scapy/sendrecv.py:L626] — falling back to `conf.iface`
[scapy/sendrecv.py:L629]; if an explicit `iface` is passed it is returned unchanged
[scapy/sendrecv.py:L631]. The gateway `0.0.0.0` means "directly connected, no next-hop router,"
which is correct for loopback.

### 3.3 Transmission — both send paths and the progress report

**Direct answer.** At the default verbosity (`conf.verb = 2`), sending prints **one `.` per
packet** on stdout, followed by the summary line **`Sent N packets.`** — and **nothing else**.
Scapy does **not** print the routing table, the route tuple, or any packet field during
transmission; the routing surfaces in §3.1–§3.2 appear only because they were requested
explicitly. The **layer-3** `send()` and the **layer-2** `sendp()` share the same *reporting*
engine, so their progress output is byte-identical — but, as the cause→effect below shows, they
select the outgoing interface **differently**. Both carry the **same** canonical packet, entered
at different layers: the L3 call sends its IP-onward layers `IP()/TCP()` (the L3 socket supplies
the Ethernet header at transmit time), while the L2 call sends the full frame
`Ether()/IP()/TCP()`. Each case was run as a separate one-shot program with **stdout and stderr
captured to separate files** (`python3 -B -u`), so the byte counts are exact.

**L3 `send()` on loopback** — command and captured streams:

```
$ python3 -B -u -c 'from scapy.all import IP,TCP,send; send(IP()/TCP(), iface="lo")'
# stdout — 18 bytes, exactly ".\nSent 1 packets.\n":
.
Sent 1 packets.
# stderr — only the import-time CryptographyDeprecationWarning (no routing table, no fields)
```

**L2 `sendp()` on loopback** — command and captured streams:

```
$ python3 -B -u -c 'from scapy.all import Ether,IP,TCP,sendp; sendp(Ether()/IP()/TCP(), iface="lo")'
# stdout — 18 bytes, exactly ".\nSent 1 packets.\n":
.
Sent 1 packets.
# stderr — only the import-time CryptographyDeprecationWarning
```

**`count=3`** — three dots, one summary line:

```
$ python3 -B -u -c 'from scapy.all import IP,TCP,send; send(IP()/TCP(), iface="lo", count=3)'
# stdout — 20 bytes, exactly "...\nSent 3 packets.\n":
...
Sent 3 packets.
```

**`verbose=0`** — **observed zero bytes** on stdout (measured, not asserted). Redirecting stdout
to a file and counting bytes shows `0` for both paths:

```
$ python3 -B -u -c 'from scapy.all import IP,TCP,send; send(IP()/TCP(), iface="lo", verbose=0)' >out 2>/dev/null; wc -c <out
0
$ python3 -B -u -c 'from scapy.all import Ether,IP,TCP,sendp; sendp(Ether()/IP()/TCP(), iface="lo", verbose=0)' >out 2>/dev/null; wc -c <out
0
```

**Cause → effect — why they report identically.** Both paths funnel through `_send()`
[scapy/sendrecv.py:L396] into the shared engine `__gen_send()` [scapy/sendrecv.py:L332]. There,
`verbose` defaults to `conf.verb` [scapy/sendrecv.py:L355]; each packet is transmitted with
`s.send(p)` [scapy/sendrecv.py:L375]; and **only if `verbose`** is truthy does it write one `.`
per packet via `os.write(1, b".")` [scapy/sendrecv.py:L380] and finally
`print("\nSent %i packets." % n)` [scapy/sendrecv.py:L392]. That single gate accounts for every
byte count above: `verbose=0` → 0 bytes; one packet → `.` + `Sent 1 packets.` (18 bytes);
`count=3` → `...` + `Sent 3 packets.` (20 bytes).

**Cause → effect — why they route differently.** The two entry points choose the outgoing
interface by different rules:

- **L3 `send()` is route-driven.** `send()` [scapy/sendrecv.py:L422] first calls
  `_interface_selection()` [scapy/sendrecv.py:L442] (which, when no explicit `iface` is given,
  resolves the first packet's route; here `iface="lo"` is passed, so it is used as-is), then
  transmits through a layer-3 socket factory `lambda iface: iface.l3socket()`
  [scapy/sendrecv.py:L445]. Critically, the layer-3 socket itself re-routes **every** packet:
  `L3PacketSocket.send` [scapy/arch/linux.py:L598] executes `iff = x.route()[0]`
  [scapy/arch/linux.py:L600] to bind each packet to the interface its own IP destination selects
  (falling back to `conf.iface` only if that route is `None` [scapy/arch/linux.py:L601-602]).
  This is why the L3 path is inherently route-driven, and why the `127.0.0.1` packet goes out `lo`.
- **L2 `sendp()` is *not* route-driven.** `sendp()` [scapy/sendrecv.py:L452] does **not** call
  `_interface_selection()`. It uses the interface you pass (`iface="lo"`); with no `iface` it
  falls back to `conf.iface` inside `_send()` [scapy/sendrecv.py:L411]; and it consults the
  routing table only when an `iface_hint` is supplied, via `conf.route.route(iface_hint)[0]`
  [scapy/sendrecv.py:L474-475]. It then transmits through a layer-2 socket factory
  `lambda iface: iface.l2socket()` [scapy/sendrecv.py:L478]. The Ethernet frame is sent as built,
  with no layer-3 route lookup for the frame itself.

Common plumbing: `_send()` resolves the chosen interface with `resolve_iface()`
[scapy/interfaces.py:L386], and the factories are `NetworkInterface.l3socket`
[scapy/interfaces.py:L165] and `l2socket` [scapy/interfaces.py:L157]. On this Linux host they
yield the native `AF_PACKET` sockets `L3PacketSocket` [scapy/arch/linux.py:L587] and `L2Socket`
[scapy/arch/linux.py:L475]; the byte-level transmit primitive is `SuperSocket.send`
[scapy/supersocket.py:L97] (its companion receive primitive, used by the sniffer in §4, is
`SuperSocket.recv` [scapy/supersocket.py:L172]). Because the destination is `127.0.0.1`, the
packet is sent on `lo` and never leaves the host.

> **Observed nuance (fd-level vs buffered output).** The `.` is written straight to file
> descriptor 1 with `os.write` (unbuffered), whereas `Sent N packets.` goes through buffered
> `print`. Under captured/piped stdout the two can interleave out of order; running with
> `python3 -u` (as every capture above does) shows them in the natural order.

---


## 4. Sniffing — Capturing the Same Traffic and Reconstructing the Layers

To capture "the same traffic," an `AsyncSniffer` is started on the loopback interface, then the
**same** canonical `Ether()/IP()/TCP()` packet from §2–§3 is sent once on `lo`. The capture is
**bounded by a `timeout`** and **correlated by a user-space `lfilter`** that matches the packet's
exact signature — loopback destination `127.0.0.1`, TCP `sport == 20` → `dport == 80`, and the
`SYN` flag. This self-correlates send and sniff on one host. Two points matter for honesty:

- **The `lfilter` runs in user space, not the kernel.** With `libpcap` absent (see §4.5) no BPF
  filter is compiled, so correlation is done by the Python predicate on every captured frame.
  The predicate checks the packet's fields *before* anything is asserted about identity, and the
  `timeout` guarantees the run ends even if nothing matches.
- **Busy-host caveat.** On a quiet host the only traffic matching that precise signature is the
  packet we sent. On a busy host, any other loopback frame with the identical 5-tuple/flags would
  also match; the `timeout` + field verification bound and qualify the result, but the correlation
  is by signature, not by a cryptographic identity.

### 4.1 What the capture returns — and how many frames

**Direct answer.** The result is a `PacketList` whose `repr` summarizes it by protocol, and each
captured packet has been **fully reconstructed into typed `Ether/IP/TCP` layers** — not left as
raw bytes. A subtle but important observation: a **bounded** capture (a `timeout`, and
**deliberately no `count=1`**) of a **single** send on loopback returns **two byte-identical
frames**, because the loopback path delivers both the *outgoing* copy and the *looped-back
incoming* copy to the `AF_PACKET` listener. Observed:

```
len(results)                       -> 2
results                            -> <Sniffed: TCP:2 UDP:0 ICMP:0 Other:0>
raw(results[0]) == raw(results[1]) -> True
[len(raw(p)) for p in results]     -> [54, 54]
[p.time for p in results]          -> two timestamps a few microseconds apart
                                      (e.g. 1784059864.261764 and .261770 -> ~6 us)
results[0].sniffed_on              -> 'lo'
results[0].haslayer(IP), results[0].haslayer(TCP) -> (True, True)
results[0][TCP].sport, results[0][TCP].dport      -> (20, 80)     # ftp_data -> http
results[0][IP].chksum, results[0][TCP].chksum     -> (0x7ccd, 0x917c)
raw(results[0]) == raw(Ether()/IP()/TCP())        -> True
results[0].summary() -> 'Ether / IP / TCP 127.0.0.1:ftp_data > 127.0.0.1:http S'
```

And `PacketList.show()` (which delegates to `nsummary()`) prints the numbered one-line list:

```
0000 Ether / IP / TCP 127.0.0.1:ftp_data > 127.0.0.1:http S
0001 Ether / IP / TCP 127.0.0.1:ftp_data > 127.0.0.1:http S
```

**Cause → effect.** The list summary `<Sniffed: TCP:2 UDP:0 ICMP:0 Other:0>` is
`PacketList.__repr__` [scapy/plist.py:L105] (the `PacketList` class is [scapy/plist.py:L741];
it also offers `summary` [scapy/plist.py:L193] and `show` [scapy/plist.py:L236], the latter
delegating to `nsummary`). The **`sniffed_on = 'lo'`** metadata is attached during capture at
[scapy/sendrecv.py:L1252] (`p.sniffed_on = sniff_sockets[s]`). Live capture is driven by
`AsyncSniffer` [scapy/sendrecv.py:L981] (worker `_run` at [scapy/sendrecv.py:L1064]); the
synchronous `sniff()` [scapy/sendrecv.py:L1308] is the same machinery.

**Why two frames (not one).** The two captured frames are **byte-identical** (`raw()` equal),
both **54 bytes**, with timestamps only a few microseconds apart — one is the packet as the
`AF_PACKET` socket sees it leave (`PACKET_OUTGOING`) and the other is the same packet looped
back and received on `lo`. Passing `count=1` would stop after the first and **hide** this
loopback duplication; the bounded (`timeout`) run *without* `count` reveals the true multiplicity.
This is a property of loopback capture, not of the packet.

**Proof it is the same packet.** `raw(results[0]) == raw(Ether()/IP()/TCP())` is `True`, and the
captured IP/TCP checksums `0x7ccd`/`0x917c` are exactly those computed by §2.4's `show2()`. The
sniffed packet is byte-for-byte the one that was built and sent.

### 4.2 The complete captured packet — `show()`

**Direct answer.** `results[0].show()` prints the full field-by-field view of the reconstructed
packet. It is **identical** to §2.4's `show2()` (same `ihl=5`, `len=40`, IP `chksum=0x7ccd`,
`dataofs=5`, TCP `chksum=0x917c`), which is the strongest confirmation that build → send → sniff
carried one unchanged packet. Complete, unedited:

```
###[ Ethernet ]### 
  dst       = ff:ff:ff:ff:ff:ff
  src       = 00:00:00:00:00:00
  type      = IPv4
###[ IP ]### 
     version   = 4
     ihl       = 5
     tos       = 0x0
     len       = 40
     id        = 1
     flags     = 
     frag      = 0
     ttl       = 64
     proto     = tcp
     chksum    = 0x7ccd
     src       = 127.0.0.1
     dst       = 127.0.0.1
     \options   \
###[ TCP ]### 
        sport     = ftp_data
        dport     = http
        seq       = 0
        ack       = 0
        dataofs   = 5
        reserved  = 0
        flags     = S
        window    = 8192
        chksum    = 0x917c
        urgptr    = 0
        options   = ''
```

### 4.3 How raw bytes become typed layers (dissection)

**Direct answer.** On capture, the raw bytes are dissected top-down: each layer decides the class
of the next layer from the protocol-binding registry, reversing exactly what stacking did during
the build.

The dissected packet's `repr` is **much denser** than the freshly built one — it prints every
**scalar** field it parsed off the wire — but note it is *not* literally "every field": the empty
`IP.options` list is **still suppressed** (see §4.4). Observed:

```
<Ether  dst=ff:ff:ff:ff:ff:ff src=00:00:00:00:00:00 type=IPv4 |<IP  version=4 ihl=5 tos=0x0 len=40 id=1 flags= frag=0 ttl=64 proto=tcp chksum=0x7ccd src=127.0.0.1 dst=127.0.0.1 |<TCP  sport=ftp_data dport=http seq=0 ack=0 dataofs=5 reserved=0 flags=S window=8192 chksum=0x917c urgptr=0 |>>>
```

**Cause → effect.** The raw bytes enter through `Packet.__init__(_pkt=...)`
[scapy/packet.py:L141] and are parsed by `dissect()` [scapy/packet.py:L1049] (which drives
`do_dissect()` [scapy/packet.py:L1002] and `extract_padding()` [scapy/packet.py:L982]). The
crucial step is choosing each next layer: `guess_payload_class()` [scapy/packet.py:L1062]
consults the `payload_guess` entries populated by the same `bind_layers` calls used for the
build — `Ether.type == 2048` selects `IP` [scapy/layers/inet.py:L1101] and `IP.proto == 6` with
`frag == 0` selects `TCP` [scapy/layers/inet.py:L1113]. If no binding matched, it would fall back
to `default_payload_class()` [scapy/packet.py:L1081] → `conf.raw_layer` [scapy/packet.py:L1090]
(a raw byte blob) — but here every layer matched, so `Ether/IP/TCP` is fully rebuilt.

### 4.4 The key contrast: built `repr` vs dissected `repr`

**Direct answer.** This is the clearest demonstration of Scapy's selective display: the **same
logical packet** prints *few* fields when freshly built, but *many more* once captured and
dissected — though **not** literally every field, because the empty-collection guard from §2.2
still applies.

```
BUILT   repr(Ether()/IP()/TCP())
  -> <Ether  type=IPv4 |<IP  frag=0 proto=tcp |<TCP  |>>>

SNIFFED repr (the dissected packet from §4.3)
  -> <Ether  dst=ff:ff:ff:ff:ff:ff src=00:00:00:00:00:00 type=IPv4 |<IP  version=4 ihl=5 tos=0x0 len=40 id=1 flags= frag=0 ttl=64 proto=tcp chksum=0x7ccd src=127.0.0.1 dst=127.0.0.1 |<TCP  sport=ftp_data dport=http seq=0 ack=0 dataofs=5 reserved=0 flags=S window=8192 chksum=0x917c urgptr=0 |>>>
```

**Cause → effect.** The **built** packet's `repr` shows only what was set or overloaded
(non-empty), skipping every plain default at [scapy/packet.py:L569]. The **dissected** packet is
far denser because `dissect()` [scapy/packet.py:L1049] populates `self.fields` with every
**scalar** value it parsed off the wire, so each of those passes the membership test at
[scapy/packet.py:L559] and prints. **But it is not "every field":** the parsed `IP.options` is an
empty list — present in `self.fields` as `[]`, yet dropped by the empty-collection guard at
[scapy/packet.py:L561-562] (verified: `results[0][IP].fields` contains `'options': []`, and the
IP `repr` shows **no** `options`). Same `__repr__` logic [scapy/packet.py:L552]; the difference
in density is entirely due to how many fields are populated — and the **complete** field view
(including the empty collection) is `show()` in §4.2, not `repr`.

### 4.5 libpcap absent — the observed error, and why capture still works

**One transcript, both streams.** Supplying a BPF `filter=` argument while `libpcap` is absent
prints an error to **stderr**, yet the native capture on **stdout** still succeeds. Running
`AsyncSniffer(iface="lo", filter="tcp and dst host 127.0.0.1", timeout=3)` with a concurrent send:

```
# stderr:
ERROR: Cannot set filter: libpcap is not available. Cannot compile filter !
# stdout — native AF_PACKET capture still returned the packet(s):
len(results)          -> 2
results               -> <Sniffed: TCP:2 UDP:0 ICMP:0 Other:0>
results[0].sniffed_on -> 'lo'
results[0][TCP].dport -> 80
results[0].summary()  -> 'Ether / IP / TCP 127.0.0.1:ftp_data > 127.0.0.1:http S'
```

**Cause → effect.** Compiling a BPF filter expression requires `libpcap`/`tcpdump`, which are
**not installed** here, so the compile step fails and prints the error. But sniffing does not
depend on libpcap: Scapy captures through the native Linux `AF_PACKET` listener `L2ListenSocket`
[scapy/arch/linux.py:L579] (a subclass of `L2Socket` [scapy/arch/linux.py:L475], reading frames
via `recv_raw()` [scapy/arch/linux.py:L555]). The socket simply receives **unfiltered**, so the
`filter=` argument has no effect and the user-space `lfilter` (§4) is what actually correlates
the packet. This is an environment fact, not a defect — the BPF filter is skipped, capture
proceeds.

---

## 5. Summary — What Scapy Revealed at Each Stage

Following one `Ether()/IP()/TCP()` packet through its whole lifecycle, Scapy revealed a
consistent design at every stage:

- **BUILD.** The `/` operator [scapy/packet.py:L596] stacks layers, and **protocol bindings
  drive field overloading**: `bind_layers` [scapy/layers/inet.py:L1101, L1113] silently set
  `type=IPv4`, `proto=tcp`, and `frag=0` so the stack is internally consistent without the user
  touching them. The display is deliberately **selective and pre-serialization**:
  `__repr__` [scapy/packet.py:L552] shows only set or overloaded (non-empty) fields, and `show()`
  [scapy/packet.py:L1459] leaves auto-computed fields (`ihl`, `len`, checksums, `dataofs`) as
  `None`. Those values are **deferred until the packet is built to bytes** — `show2()`
  [scapy/packet.py:L1473] / `raw()` [scapy/compat.py:L112] run `post_build`
  [scapy/packet.py:L758] to compute them, and `hexdump()` [scapy/utils.py:L273] confirms the
  final 54-byte wire form.

- **SEND.** The two paths select their interface **differently**. Layer-3 `send()`
  [scapy/sendrecv.py:L422] is **route-driven**: Scapy resolves `(iface, output_ip, gateway_ip)`
  via `IP.route()` [scapy/layers/inet.py:L559] → `Route.route()` [scapy/route.py:L146], and both
  `_interface_selection()` [scapy/sendrecv.py:L616] and the layer-3 socket
  (`L3PacketSocket.send` → `x.route()[0]` [scapy/arch/linux.py:L600]) use that route. Layer-2
  `sendp()` [scapy/sendrecv.py:L452] is **not** route-driven — it sends on the interface given (or
  `conf.iface`). The routing table itself (`Route.__repr__` [scapy/route.py:L47]) is shown **only
  when requested**, never emitted by `send`/`sendp`. Both paths report through **one shared
  engine** `__gen_send()` [scapy/sendrecv.py:L332], whose terse `verb=2` output is a `.` per
  packet [scapy/sendrecv.py:L380] and a final `Sent N packets.` [scapy/sendrecv.py:L392].

- **SNIFF.** Capture uses the native Linux `AF_PACKET` listener `L2ListenSocket`
  [scapy/arch/linux.py:L579], and the raw bytes are **reconstructed into typed layers** by the
  *same* binding registry used to build them: `dissect()` [scapy/packet.py:L1049] +
  `guess_payload_class()` [scapy/packet.py:L1062] walk `Ether → IP → TCP` and, because
  dissection populates every **scalar** field, the packet's `repr` is now much **denser** than the
  sparse built-packet `repr` (though the empty `IP.options` list is still hidden by the
  empty-collection guard [scapy/packet.py:L561-562]; the complete field view is `show()`). Capture
  even succeeds with `libpcap` absent (the BPF filter is skipped with an error, but the native
  socket still receives — and a loopback send is captured **twice**, outgoing plus looped-back).

**In one sentence:** Scapy builds by *overloading* fields through bindings and *deferring*
computed values until serialization; it sends by *resolving a route* at layer 3 (layer 2 sends on
the given interface) and reporting tersely through one engine; and it sniffs by *capturing raw
bytes* and *re-dissecting* them through the same bindings — so the packet you built, sent, and
captured is provably the same one (`raw(captured) == raw(Ether()/IP()/TCP())`), shown sparsely
when constructed and much more densely when parsed, with `show()` giving the complete field view.

### 5.1 Inferred edge case — no matching route

**Labeled inferred (not triggered here).** Because a default route (`0.0.0.0`) is present in
this environment, `Route.route()` always finds a match, so the no-route branch could not be
exercised. From reading the source: when no route matches, `Route.route()` emits
`warning("No route found (no default route?)")` [scapy/route.py:L189] and returns the loopback
fallback `(conf.loopback_name, "0.0.0.0", "0.0.0.0")` [scapy/route.py:L190]. This is derived
from code inspection, not observed output.

---

## Appendix — Reproduction

Every observation above was produced by running the programs below against the Scapy **source
tree** at commit `0925ada485406684174d6f068dbd85c4154657b3`, under **Python 3.13.7 as root**. Each
program prepends the repo root to `sys.path` (so Scapy is imported from source), strips ANSI
escapes with `re.sub(r'\x1b\[[0-9;]*m', '', s)`, and **writes nothing into the repository**. The
scripts were kept under `/tmp` (outside the checkout) and removed afterward. Run each from the
repository root.

### A. Banner / version

```
echo "exit()" | python3 -m scapy
```

stdout is the 17-byte ANSI-colored `>>> ` prompt; the banner and `Version 2026.07.14` go to
stderr (see §1).

### B. Build — `python3 -B -u /tmp/obs_build.py`

```python
#!/usr/bin/env python3
"""Observation script: BUILD stage of Ether()/IP()/TCP().
Run from repo root:  python3 -B -u /tmp/obs_build.py
Imports Scapy from the source tree; strips ANSI for clean text capture.
Writes nothing into the repository."""
import io
import os
import re
import sys

sys.path.insert(0, os.getcwd())
from scapy.all import Ether, IP, TCP, conf, hexdump  # noqa: E402
import scapy  # noqa: E402

ANSI = re.compile(r"\x1b\[[0-9;]*m")
def strip(s): return ANSI.sub("", s)

print("scapy.VERSION =", scapy.VERSION)
print("conf.verb     =", conf.verb)
print("conf.iface    =", conf.iface)
print("color_theme   =", type(conf.color_theme).__name__)
print("python        =", sys.version.split()[0])
print()

print("== 2.2 incremental repr ==")
print("repr(Ether())            ->", strip(repr(Ether())))
print("repr(Ether()/IP())       ->", strip(repr(Ether()/IP())))
print("repr(Ether()/IP()/TCP()) ->", strip(repr(Ether()/IP()/TCP())))
print()

eit = Ether()/IP()/TCP()
print("== 2.2 overloaded_fields / fields ==")
print("eit[Ether].overloaded_fields =", eit[Ether].overloaded_fields)
print("eit[Ether].fields            =", eit[Ether].fields)
print("eit[IP].overloaded_fields    =", eit[IP].overloaded_fields)
print("eit[IP].fields               =", eit[IP].fields)
print("eit[TCP].overloaded_fields   =", eit[TCP].overloaded_fields)
print("eit[TCP].fields              =", eit[TCP].fields)
print()

print("== 2.3 summary / command ==")
print("summary() ->", repr(strip((Ether()/IP()/TCP()).summary())))
print("command() ->", repr((Ether()/IP()/TCP()).command()))
print()

print("== 2.4 show() (pre-build, in-memory) — COMPLETE ==")
print(strip((Ether()/IP()/TCP()).show(dump=True)))
print("== end show() ==")
print()

print("== 2.4 show2() (post-build, re-dissected) — COMPLETE ==")
print(strip((Ether()/IP()/TCP()).show2(dump=True)))
print("== end show2() ==")
print()

print("== 2.5 hexdump / len ==")
print("len(bytes(Ether()/IP()/TCP())) =", len(bytes(Ether()/IP()/TCP())))
print(strip(hexdump(Ether()/IP()/TCP(), dump=True)))
print("== end hexdump ==")
```

### C. Send — `python3 -B -u /tmp/obs_send.py`

```python
#!/usr/bin/env python3
"""Observation: SEND stage (routing + both transmit paths).
Run from the repo root:  python3 -B -u /tmp/obs_send.py
The -u flag keeps the per-packet '.' and the 'Sent N packets.' line in order.
Reads Scapy from the source tree; writes nothing into the repository."""
import os, re, sys
sys.path.insert(0, os.getcwd())
from scapy.all import Ether, IP, TCP, conf, send, sendp  # noqa: E402
strip = lambda s: re.sub(r"\x1b\[[0-9;]*m", "", s)

# Routing info is printed ONLY because we ask for it here; send/sendp never emit it.
print(strip(repr(conf.route)))                 # routing table
print(IP(dst="127.0.0.1").route())             # ('lo', '127.0.0.1', '0.0.0.0')
print(conf.route.route("127.0.0.1"))           # same tuple, via Route.route()

# Transmission (default conf.verb == 2): one '.' per packet then 'Sent N packets.'
send(IP() / TCP(), iface="lo")                 # layer 3 (IP-onward layers)
sendp(Ether() / IP() / TCP(), iface="lo")      # layer 2 (full Ethernet frame)
send(IP() / TCP(), iface="lo", count=3)        # -> '...' then 'Sent 3 packets.'
send(IP() / TCP(), iface="lo", verbose=0)      # silent: zero bytes on stdout
sendp(Ether() / IP() / TCP(), iface="lo", verbose=0)  # silent
```

The `-u` (unbuffered) flag keeps the per-packet `.` (written to fd 1 via `os.write`) in order with
the buffered `Sent N packets.` line. For the exact per-case byte counts (18 / 18 / 20 / 0 / 0) run
each call in its own process and redirect stdout, e.g.
`python3 -B -u -c 'from scapy.all import IP,TCP,send; send(IP()/TCP(), iface="lo", verbose=0)' >out 2>/dev/null; wc -c <out`.

### D. Sniff — `python3 -B -u /tmp/obs_sniff.py`

```python
#!/usr/bin/env python3
"""Observation: SNIFF stage — capture the canonical Ether()/IP()/TCP() on lo.
Bounded by timeout, correlated by a full-signature lfilter (NO count=1), so we
observe the ACTUAL loopback multiplicity. Run: python3 -B -u /tmp/obs_sniff.py"""
import os, re, sys, time, threading
sys.path.insert(0, os.getcwd())
from scapy.all import AsyncSniffer, Ether, IP, TCP, sendp, raw  # noqa: E402
ANSI = re.compile(r"\x1b\[[0-9;]*m")
def strip(s): return ANSI.sub("", s)

pkt = Ether()/IP()/TCP()   # the SAME canonical packet used in build & send

def lf(p):
    # Full-signature correlation: loopback dst, TCP sport 20 -> dport 80, SYN.
    return (IP in p and p[IP].dst == "127.0.0.1"
            and TCP in p and p[TCP].sport == 20
            and p[TCP].dport == 80 and p[TCP].flags == "S")

t = AsyncSniffer(iface="lo", lfilter=lf, timeout=3)
t.start()
time.sleep(0.5)
sendp(pkt, iface="lo", verbose=0)     # transmit ONCE
t.join()
results = t.results

print("== 4.1 capture result object ==")
print("type(results)     =", type(results).__name__)
print("len(results)      =", len(results))
print("repr(results)     =", strip(repr(results)))
print()
print("== 4.x ACTUAL loopback multiplicity (no count=1) ==")
print("frames captured   =", len(results))
if len(results) >= 2:
    print("raw(r[0])==raw(r[1]) =", raw(results[0]) == raw(results[1]))
    print("len(raw) each     =", [len(raw(p)) for p in results])
    print("times             =", ["%.6f" % p.time for p in results])
print()
print("== 4.1 PacketList.show()/nsummary() (numbered, prints directly) ==")
results.show()
print("== end PacketList.show() ==")
print()
r0 = results[0]
print("== 4.2 first captured packet: full show() ==")
print(strip(r0.show(dump=True)))
print("== end packet show() ==")
print()
print("== 4.3 dissected repr (denser, but empty collections still hidden) ==")
print("repr(r0)          =", strip(repr(r0)))
print("r0[IP].fields keys=", list(r0[IP].fields.keys()))
print("'options' in r0[IP].fields =", "options" in r0[IP].fields,
      "-> value =", r0[IP].fields.get("options"))
print("'options' shown in repr?   =", "options" in strip(repr(r0)))
print()
print("== 4.2 correlation / metadata ==")
print("sniffed_on        =", r0.sniffed_on)
print("haslayer(TCP)     =", r0.haslayer(TCP))
print("r0[TCP].sport     =", r0[TCP].sport)
print("r0[TCP].dport     =", r0[TCP].dport)
print("r0[TCP].flags     =", r0[TCP].flags)
print("r0[IP].chksum     =", hex(r0[IP].chksum))
print("r0[TCP].chksum    =", hex(r0[TCP].chksum))
print("raw(r0)==raw(pkt) =", raw(r0) == raw(pkt))
print("summary()         =", repr(strip(r0.summary())))
```

This is bounded by `timeout=3` and correlated by a user-space `lfilter` on the packet's exact
signature; it uses **no** `count=1`, so it reveals the true loopback multiplicity (two
byte-identical frames).

### E. BPF filter with libpcap absent — `python3 -B -u /tmp/obs_sniff_bpf.py`

```python
#!/usr/bin/env python3
"""Observation: sniff() with a BPF filter= while libpcap is ABSENT.
Proves the stderr error AND that native AF_PACKET capture still succeeds.
Run: python3 -B -u /tmp/obs_sniff_bpf.py"""
import os, re, sys, time
sys.path.insert(0, os.getcwd())
from scapy.all import AsyncSniffer, Ether, IP, TCP, sendp  # noqa: E402
ANSI = re.compile(r"\x1b\[[0-9;]*m")
def strip(s): return ANSI.sub("", s)

pkt = Ether()/IP()/TCP()
t = AsyncSniffer(iface="lo", filter="tcp and dst host 127.0.0.1", timeout=3)
t.start()
time.sleep(0.5)
sendp(pkt, iface="lo", verbose=0)
t.join()
results = t.results
print("== 4.4 native capture succeeded despite BPF failure ==")
print("len(results)  =", len(results))
print("repr(results) =", strip(repr(results)))
print("iface         =", results[0].sniffed_on if results else None)
print("dport         =", results[0][TCP].dport if results else None)
print("summary       =", repr(strip(results[0].summary())) if results else None)
```

stderr shows `ERROR: Cannot set filter: libpcap is not available. Cannot compile filter !`; stdout
shows the native `AF_PACKET` capture still succeeded (see §4.5).

### F. Repository integrity

The only file this task adds is the answer document; no Scapy source, test, configuration, or
build file is modified. Verified against the source baseline:

```
$ git branch --show-current
blitzy-25029afa-3880-4f96-a5df-88edf3ddbb3d

$ git diff --name-status 0925ada485406684174d6f068dbd85c4154657b3 -- .
A	blitzy/documentation/scapy_0925ada48540.md

$ git diff --stat 0925ada485406684174d6f068dbd85c4154657b3 -- .
 blitzy/documentation/scapy_0925ada48540.md | 978 +++++++++++++++++++++++++++++
 1 file changed, 978 insertions(+)
```

- **Branch:** `blitzy-25029afa-3880-4f96-a5df-88edf3ddbb3d`.
- **Source baseline commit:** `0925ada485406684174d6f068dbd85c4154657b3`; the document is added on
  top of it by the commit that delivers this file (HEAD of the branch).
- **Only added path:** `blitzy/documentation/scapy_0925ada48540.md` — the `git diff --name-status`
  against the baseline lists exactly one entry (`A`), and nothing under `scapy/`, `test/`,
  `pyproject.toml`, or `tox.ini`.
- **Temporary scripts** (`/tmp/obs_build.py`, `/tmp/obs_send.py`, `/tmp/obs_sniff.py`,
  `/tmp/obs_sniff_bpf.py`) live outside the repository and were removed after use, so a final
  `git status --porcelain` reports only the single documentation file before commit and an empty
  (clean) working tree after it.

As noted in the introduction, several values are host-, run-, or version-specific and are **not**
universal: the banner's rotating quote; the routing-table interfaces/IPs (`10.236.0.*` here); the
Python version (`3.13.7`); which optional packages are present (IPython and `libpcap` absent,
`cryptography` present); the absolute repo path shown in warnings; the socket backend (native
`AF_PACKET` here); and the service names (`ftp_data`/`http`). Scoped to **this checkout, this host,
and the exact `Ether()/IP()/TCP()` packet**, the structural values reproduce on repeated runs: the
reprs, the checksums `0x7ccd` (IP) and `0x917c` (TCP), the 54-byte length, the `Sent N packets.`
lines, and the dissected fields. The number of frames a loopback capture returns (**two** here) is
a property of loopback, not of the packet, and would differ on a non-loopback interface.
